{
  "status": "partial",
  "result": "Codex gpt-6-astra A5, status partial: I inspected the local sources and exercised codex 0.153.4’s configuration resolver, establishing read-only networking controls while leaving approval privileges and persistence unresolved.\n1. STATIC PROFILE — confidence: read directly. Yes: permissions.<name>.network accepts a TOML table. Verified network={enabled=true} and {enabled=false}; a bare boolean is rejected as expecting NetworkToml. Access-related grammar also includes mode=\"limited\"|\"full\" and domains={\"host\"=\"allow\"|\"deny\"}. These findings come from binary parser diagnostics and config/read, not the different JSON-RPC permission-request object. Other proxy/socket fields are recorded in the artifact; this is not an exhaustive nested grammar listing or proof of domain enforcement.\n2. WORKSPACE-WRITE — confidence: read directly. Egress is not structurally tied to workspace-write. A named profile extending \":read-only\" with network.enabled=true resolves to readOnly/networkAccess=true. The attempted analogous key sandbox_read_only.network_access is rejected by --strict-config; sandbox_workspace_write.network_access=true leaves legacy read-only networking false. The driver’s restriction of --network to write level is its own policy.\n3. WHAT accept GRANTS — confidence: not established. The pinned description says only “User approved the command”; it specifies neither retained sandbox restrictions nor complete sandbox removal. Evidence that granting can “step outside the sandbox the caller chose” is second-hand from a driver comment, not a demonstrated universal meaning of accept. The schema separately recognizes sandbox escapes and blocked-network approvals. Neither blanket unsandboxing nor host-only permission can be concluded. Generated experimental fields add information absent from the pinned contract; the pinned contract takes priority.\n4. PERSISTENCE — confidence: not established. The schema calls applyNetworkPolicyAmendment persistent and specifies host plus action=allow|deny, but gives no storage location, expiration, or turn/thread/home boundary. Survival after a seat exits and inheritance by unrelated seats remain unknown. Directly read driver code reuses stateDir()/home across runs and rewrites config.toml; neither fact identifies where amendments live.\n5. WHEN THE HOST IS PRESENT — confidence: read directly. networkApprovalContext is optional and nullable, described only as “Optional context for a managed-network approval prompt.” When supplied, its object requires host and protocol; protocol is http|https|socks5Tcp|socks5Udp. There is no promise that every blocked network command generates an approval or carries this context. The always-named-host assumption is unsupported by the contract.\n6. READ-ONLY PLUS NETWORK — confidence: read directly. The pinned SandboxPolicy explicitly represents {type:\"readOnly\",networkAccess:true}, and the installed binary returned exactly that effective sandbox for the named profile. Adding the driver’s tmpdir write grant instead returned workspaceWrite with that writable root and networkAccess=true. The decoded MDM permits read-only/workspace-write and declares no network restriction. These are representation and policy-resolution findings; outbound connectivity was not tested.",
  "evidence": [
    "1. /private/var/folders/mf/9v804k_57lq30kwtlr7468xr0000gn/T/codex-A5-network.tQvDEe/evidence.txt:4 records the exact codex --strict-config ... app-server --stdio command; :6 records the resolved network table; :7 and :13 show enabled true/false taking effect. :50–57 record parser commands and errors: 'expected struct NetworkToml', 'expected a boolean', 'expected allow or deny', and 'expected limited or full'. :33–35 record accepted domain rules. schema-0.153.4/ServerRequest.json:82 identifies the separate RPC object as a per-command overlay.",
    "2. /private/var/folders/mf/9v804k_57lq30kwtlr7468xr0000gn/T/codex-A5-network.tQvDEe/evidence.txt:22–24 records the command and 'unknown configuration field sandbox_read_only'; :27–30 records sandbox_workspace_write.network_access=true alongside resulting readOnly/networkAccess=false. schema-0.153.4/v2/ConfigReadResponse.json:641 and :998 define the workspace-write setting. skills/seat/scripts/driver.mjs:2065 rejects --level read --network.",
    "3. schema-0.153.4/ServerRequest.json:269 describes accept solely as 'User approved the command.' schema-0.153.4/ServerNotification.json:468 lists sandbox escapes and blocked network access separately; :2579 distinguishes network-call reviews from command-execution reviews. skills/seat/scripts/driver.mjs:2501 supplies the second-hand 'step outside' statement. Ran codex --help, codex exec --help, codex sandbox --help, codex debug --help, and codex app-server --help; none defines RPC accept semantics. Experimental-schema comparison is recorded in /private/var/folders/mf/9v804k_57lq30kwtlr7468xr0000gn/T/codex-A5-network.tQvDEe/evidence.txt:81.",
    "4. schema-0.153.4/ServerRequest.json:309 says 'persistent network policy rule'; :432 says 'for future requests'; :1533–1553 define host/action and allow/deny without storage or lifetime. skills/seat/scripts/driver.mjs:1207 selects the shared home and :1239–1264 rewrites config.toml. Ran rg -n 'sandbox|network|permissions|approval' /Users/ruliny/.codex/config.toml: no matches; no ~/.codex/rules directory was present. Neither observation establishes the amendment store.",
    "5. schema-0.153.4/ServerRequest.json:410–419 permits a context object or null and describes it as optional managed-network context; :460–464 excludes it from required parameters. :1509–1530 requires host/protocol within the supplied object and enumerates the four protocols.",
    "6. schema-0.153.4/v2/ThreadStartResponse.json:1042–1057 defines readOnly with boolean networkAccess, default false. /private/var/folders/mf/9v804k_57lq30kwtlr7468xr0000gn/T/codex-A5-network.tQvDEe/evidence.txt:7 records readOnly/networkAccess=true; :19 records the tmpdir-write profile result. Ran plutil -extract requirements_toml_base64 raw -o - '/Library/Managed Preferences/com.openai.codex.plist' | base64 --decode: allowed_sandbox_modes=['read-only','workspace-write'], with no network requirement; decoded output is preserved at :78."
  ],
  "artifacts": [
    "/private/var/folders/mf/9v804k_57lq30kwtlr7468xr0000gn/T/codex-A5-network.tQvDEe/evidence.txt",
    "/private/var/folders/mf/9v804k_57lq30kwtlr7468xr0000gn/T/codex-A5-network.tQvDEe/probe_rpc.py",
    "/private/var/folders/mf/9v804k_57lq30kwtlr7468xr0000gn/T/codex-A5-network.tQvDEe"
  ],
  "open": [
    "Q3: Observe an actual item/commandExecution/requestApproval answered with result={\"decision\":\"accept\"}, separately for sandbox escalation and managed-network approval. Trace the approved process’s sandbox policy; have that same process attempt exclusive creation of a fresh sentinel outside its writable roots and connection to a second controlled host. Record filesystem denial, second-host approval/denial, and whether the process was relaunched without sandboxing. No such approval was exercised here.",
    "Q4: On a captured managed-network approval, submit result={\"decision\":{\"applyNetworkPolicyAmendment\":{\"network_policy_amendment\":{\"host\":\"<captured host>\",\"action\":\"allow\"}}}}. Snapshot and diff the disposable CODEX_HOME and project files, including SQLite/WAL contents. Repeat that host in the same turn, next turn, new thread, restarted server with the same home, later driver seat using the shared home, and fresh home. Observe the changed store and which boundaries preserve the rule; repeat for deny.",
    "Q1/Q6: Actual egress and host-rule enforcement remain unmeasured. With a controlled reachable endpoint, run a command under the verified read-only profile and record both connectivity and attempted writes outside its grants. For domains rules, observe an allowed host and a denied host with the managed-network proxy active; successful parsing alone cannot establish enforcement.",
    "Q5: Actual population conditions remain unmeasured. Capture raw approval parameters for an OS sandbox network denial, an explicit escalation request, and a managed-proxy host denial, recording whether each produces an approval and whether networkApprovalContext is absent, null, or populated."
  ]
}