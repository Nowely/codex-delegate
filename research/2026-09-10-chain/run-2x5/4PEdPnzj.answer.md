{
  "status": "done",
  "result": "Codex gpt-6-astra A4: done, refuted D1–D5 against the 0.12.0 driver, skill contracts and pinned protocol, and ran its 139-test protocol suite.\nC1. D1 contradicts the rights contract. The read promise is “read any readable path, run commands, write only $TMPDIR” (skills/seat/SKILL.md:88); egress is “each an explicit opt-in” (README.md:172), requiring settlement “with the user” (skills/seat/SKILL.md:92). The driver rejects read-plus-network at skills/seat/scripts/driver.mjs:2065 and unexpected read egress at :1977. D1 can preserve local write restrictions while silently adding the ability to transmit anything readable and affect remote services.\nC2. The parity argument does not establish compatibility. “writes from HEAD without implicit network” concerns worktree seats and is not itself contradicted by a read-only change; “Install egress is separate from the base isolation choice” reinforces explicit separation (skills/seat/references/parity.md:15,45). Its permission row promises “none — refused, recorded, exit 6” and user-settled widening (:28), which D3/D4 replace. README.md:20’s “the default wins” shares a paragraph with rights declared per call (:17); it does not erase that promise.\nC3. D1 requires a parser and compatibility change, not a default flip: negative booleans currently omit the flag exactly like an absent line (skills/seat/scripts/driver.mjs:625). Set network true by default without preserving an explicit negative and NETWORK: no also grants egress. The CLI currently has only positive --network (:687), while explicit CLI flags are promised to override headers (:2009). Negative precedence, direct CLI calls and resumed calls need defined semantics.\nC4. D1 also undermines D4 and D5. With ambient egress enabled, an ordinary command can contact a host outside the approval ceiling without raising a request; D4 never examines it. The driver represents ambient egress as a boolean and handles approvals only when requests arrive (skills/seat/scripts/driver.mjs:2140,2622). Thus no reviewer can preserve today’s refusal handler, but cannot preserve today’s overall behavior: operations previously refused may now run without any escalation record.\nC5. D4’s universal host premise is false in the pinned protocol. networkApprovalContext is optional and nullable (schema-0.153.4/ServerRequest.json:410); generic permission requests can ask for network.enabled with no host whatsoever (:50,1666,1723). A reason mentioning network does not turn a generic command approval into a host-scoped grant. Refuse such cases until the actual supported request and response forms are specified.\nC6. Even a supplied host plus command cannot establish a public-web read. NetworkApprovalContext contains host and protocol, without HTTP method, URL path, body, headers or credentials (schema-0.153.4/ServerRequest.json:1509). A command invoking a mutable script or config file hides those choices; inherited environment is another input (skills/seat/scripts/driver.mjs:3636). The approver cannot establish payload confidentiality, remote mutations, redirect destinations or the actual effects of subsequently executed code from those two strings.\nC7. D4 leaves the grant’s scope and lifetime unproved. The protocol offers command acceptance, session-cached acceptance, executable-policy amendments and persistent host rules (schema-0.153.4/ServerRequest.json:269,276,284,309). These are materially different permissions. Persistent rules conflict with “Never add allow-rules” (skills/seat/SKILL.md:67); resumed calls declare rights again (skills/seat/references/parity.md:20). Demonstrate that the chosen response preserves filesystem restrictions and expires within the declared call; a host label proves neither.\nC8. Granting changes the meaning of escalation throughout the driver: its policy statement (:20), escalation-array definition (:2385), inclusive failure comment and insertion (:2501–2510), and exit-decision rationale (:3262) all mean refused permission requests, not merely requested permissions (skills/seat/scripts/driver.mjs). Keep granted requests in that array and otherwise successful work still exits 6; remove them without a separate audit trail and exercised rights disappear. Requests, grants, refusals, expiry and execution require distinct records.\nC9. The ladder must remain evidence-derived. Its order is 3 → 2 → 1 → 7 → 6 → 12 → 9 → 5 → 8 → 13 → 0; transport failure can override it with 4 (skills/seat/scripts/driver.mjs:228,3264,3288,3328). A grant cannot override a timeout, failed turn, human interaction or failed verification. Conversely, an unanswered approval cut by the idle timer becomes 3, not 6. D3/D4 define neither that distinction nor whether an earlier refusal remains failure after later recovery.\nC10. There is a second dependency on refusal evidence: commandsRan counts every recorded command because “a declined one is already a higher rung” (skills/seat/scripts/driver.mjs:3062). Clearing or failing to record refusals can therefore let declined items satisfy the default command floor. Failed commands and patches have no independent failure rung (:267); successful expectations still require completed, exit-zero commands (:3066,3096). Permission granted is not command executed or task verified.\nC11. Attribution must become stricter for grants. Today requests without ownership IDs are handled conservatively, and refusals from other threads are recorded inclusively (skills/seat/scripts/driver.mjs:2473,2504). That is safe for denying rights, not sufficient for granting them. Verify the connection, owned turn and actual descendant relationship; preserve root-only success evidence (:2429,2556). The current subagent flag merely means a different thread ID (:2479), not proven membership beneath this seat.\nC12. The report cannot describe dynamically exercised rights today. effectiveSandbox is captured at thread setup; network reports the original option; escalation detail is truncated to 200 characters and lacks request ID, decision, ceiling and grant scope (skills/seat/scripts/driver.mjs:3761,3348,2508). The report includes that array unchanged (:3385). An approved escape could consequently accompany an apparently unchanged sandbox. receiptOk only validates the opening session record (:2915); it does not certify the approval or its effects.\nC13. D3 lacks a caller wake-up path. Concrete sequence: the coordinator launches the documented background task, waits for completion, the seat writes an approval request, and the driver waits for an answer. The coordinator is instructed to read the report on exit and repeatedly block on TaskOutput (skills/seat/SKILL.md:32; skills/orchestrate/SKILL.md:104–105). Neither completion happens until somebody services the request. A file alone does not implement that servicing contract.\nC14. With an asynchronous wait and no further activity, the existing timer cuts the turn after 900 seconds, allows up to 10 seconds for interruption, then reports exit 3; only generated answers, commentary or partial text survive, and an incomplete worktree is preserved (skills/seat/scripts/driver.mjs:66,99,2445,3031,3114,1811). Shutdown then closes stdin, signals the child group, escalates to SIGKILL if necessary, and releases the lock (:2270,2295). D3 must record reviewer expiry distinctly and ensure it reaches this teardown path.\nC15. The claimed wait bound is not a bound. Concrete sequence: one child awaits approval while another child emits events every few seconds; each event rearms the shared idle timer, indefinitely extending the first child’s wait (skills/seat/scripts/driver.mjs:2432,2441,2626). The command cap counts root commands only (:2573), and --idle-timeout 0 disables the guard (:415). D3 needs a request deadline independent of thread activity and that option.\nC16. If “blocks” means a synchronous polling loop, the failure is worse: execution remains inside the stdout dispatcher, so timer callbacks, signal handlers and later RPC frames cannot run (skills/seat/scripts/driver.mjs:2213,2445,3848). The child remains alive and its output can stall; no timeout report is produced. A caller resorting to SIGKILL can lose the in-memory report and strand descendants, a failure already documented at skills/seat/references/environment-and-internals.md:133. Specify an asynchronous, cancellable wait.\nC17. Locks turn reviewer delay into blocked peer work. Concrete sequence: writer A holds its cwd lock and waits for a network decision; writer B starts on that cwd and exits 10; the coordinator follows the documented instruction to wait for A, while A needs the coordinator’s answer (skills/seat/scripts/driver.mjs:2112,1428; skills/orchestrate/SKILL.md:129). The lock remains held through reporting and child teardown (:2291). Read seats take no such lock, so this is specifically a write-seat failure.\nC18. Concurrent approvals need stronger identity than a file or item ID. Concrete sequence: seats A and B publish to a shared request name, B replaces A’s request, and the answer intended for A is consumed by B; alternatively a late answer is reused after resume. Separate app-servers and concurrent read seats are normal (skills/seat/references/parity.md:102,106). Multiple callbacks can also share one itemId (schema-0.153.4/ServerRequest.json:356). Require per-invocation and per-request binding, atomic publication, single consumption and stale-answer rejection, matching the existing report’s no-clobber discipline (skills/seat/scripts/driver.mjs:899).\nC19. The answer-file location is a trust boundary with no specified solution. Put it beneath the seat’s writable TMPDIR and the requesting seat can manufacture its own approval; put it in the orchestrate state directory and the coordinator’s documented Write/mkdir/redirect route is refused in headless sessions (skills/seat/SKILL.md:88; skills/orchestrate/SKILL.md:30). Mode 0700 alone does not exclude the same seat operating inside its granted root. Demonstrate a protected mailbox and an authorized caller submission path.\nC20. A header ceiling is not automatically trusted authorization. Concrete sequence: a copied header value contains a newline that introduces a broader ceiling; the parser cannot distinguish the injected field from an intended field. This exact boundary already required CLI-only authorization for VERIFY (skills/seat/scripts/driver.mjs:566–575; skills/seat/references/incidents.md:149). D4 must identify who authorizes the ceiling and enforce it outside the relayed text; skills/seat/SKILL.md:93 still forbids translating a refusal into broader rights.\nC21. Late decisions require explicit invalidation. Concrete sequence: an approval waits, SIGTERM or an idle cut sets pendingCut, and the answer file arrives during interruption grace. An added callback that simply sends acceptance can reply to an obsolete request; current request dispatch has no settled or pendingCut guard (skills/seat/scripts/driver.mjs:2622), although steering and turn completion do (:3279,2692). Pending decisions must settle once and become unusable after cancellation, completion, server death or a new invocation.\nC22. Grants also affect retry safety. Concrete sequence: approve a network command, it performs a remote action, then a retryable turn failure arrives before any command-completed item or answer. The existing retry guard sees empty command/file/message/tool collections and can replay the original turn; it does not inspect approvals (skills/seat/scripts/driver.mjs:2710–2716,2823). A granted operation with an unknown outcome must prevent that replay under the repository’s stated purpose of avoiding repeated side effects (:2708).\nC23. The design does not address the alleged loss of work. Exit 6 does not itself discard the answer: the driver persists and reports it, and the skill explicitly says to read a non-zero result’s answer (skills/seat/scripts/driver.mjs:3110,3443,3468; skills/seat/SKILL.md:188). The incident needs investigation at generation, delivery and caller interpretation. Making more runs exit 0 can conceal that defect while leaving the next refusal just as costly.\nC24. Nor does it resolve the first two failures. Forbidden live search is refused before a turn, outside D3’s approval exchange; search-provider 429 recovery is not supplied by granting socket access (skills/seat/scripts/driver.mjs:2118–2123,2806). D2 retains today’s disabled-search setting (:2127), and standing instructions still select “local shell and filesystem only” solely from webSearch (:3526–3529). Define an authorized discovery/fetch fallback, its source evidence and its failure reporting; clarify that instruction before expecting network access to produce a finished survey.\nC25. Validation: node evals/protocol.test.mjs completed with “all 139 passed” this turn. These are baseline fixture tests, including refusal, unexpected egress, child activity and ladder behavior (evals/protocol.test.mjs:4,66,210,221,466,723). They establish the current contract; they do not validate a reviewer implementation, host-only grant enforcement or the reported live-web incident.",
  "evidence": [
    "C1 — skills/seat/SKILL.md:88: “read any readable path, run commands, write only `$TMPDIR`”; README.md:172: “egress or an extra root — each an explicit opt-in”; skills/seat/SKILL.md:92: “`NETWORK: yes` and each `WRITABLE: <dir>` widen a write seat. Settle every one with the” followed at :93 by “user before adding it”; skills/seat/scripts/driver.mjs:2065: “if (opts.level === \"read\" && (opts.network || opts.writable.length))”; :2066: “--network and --writable belong to --level write”; :1977: “the sandbox has network access, which --level read never grants”.",
    "C2 — skills/seat/references/parity.md:15: “writes from HEAD without implicit network”; :45: “Install egress is separate from the base isolation choice”; :28: “none — refused, recorded, exit 6” and “widen only the rights settled with the user”; README.md:17: “Rights are declared per call so the coordinator never”; :18: “wonders what the seat may touch”; :19: “a prompt with no header at all is a read seat in the current directory”; :20: “knob and a default compete, the default wins.”",
    "C3 — skills/seat/scripts/driver.mjs:625: “A negative header value omits the flag, just as omitting the line does”; :626: “if (/^(no|false|0)$/i.test(value)) continue”; :687: “case \"--network\": o.network = true; break”; :2009: “Command-line flags are appended after the file's, so an explicit flag still wins”; evals/cli.test.mjs:315: “r.network === false” explicitly checks the negative-header case.",
    "C4 — skills/seat/scripts/driver.mjs:1956: “Egress is opt-in and the flag is the whole of the opt-in; a server granting it unasked is a widening”; :2140: “[\"sandbox_workspace_write.network_access\", String(Boolean(opts.network))]”; :2622: “if (msg.id !== undefined && msg.method) { touchIdle(); return handleServerRequest(msg); }”. These establish that ambient egress and approval handling are separate mechanisms.",
    "C5 — schema-0.153.4/ServerRequest.json:410 defines “networkApprovalContext”; :416 permits “null”; :419 calls it “Optional context for a managed-network approval prompt”; :460 lists required fields as itemId, startedAtMs, threadId and turnId; :50 defines AdditionalNetworkPermissions with the optional boolean/null “enabled” at :52; :1681 references RequestPermissionProfile; :1736 permits its network component without a host field.",
    "C6 — schema-0.153.4/ServerRequest.json:1509 defines NetworkApprovalContext with “host” at :1511 and “protocol” at :1514; :362 describes command as “The command to be executed”; :370 describes commandActions as “Best-effort parsed command actions for friendly display”; skills/seat/scripts/driver.mjs:3636: “env: { ...process.env”; skills/seat/SKILL.md:88: “read any readable path” and “write only `$TMPDIR`”. The missing HTTP and execution-state information is a limitation of these fields, not a claim that a particular upload occurred.",
    "C7 — schema-0.153.4/ServerRequest.json:269: “User approved the command”; :276: “future prompts in the same session-scoped approval cache should run without prompting”; :284: “apply the proposed execpolicy amendment so future matching commands can run without prompting”; :309: “a persistent network policy rule (allow/deny) for this host”; skills/seat/SKILL.md:67: “Never add allow-rules on the user's behalf”; skills/seat/references/parity.md:20: “rights are declared again per call”; skills/seat/scripts/driver.mjs:3731: “the sandbox object is byte-identical under \"user\" and” followed by “\"auto_review\"” at :3732.",
    "C8 — skills/seat/scripts/driver.mjs:20: “an escalation request means the sandbox was sized wrong for the task”; :2385: “const escalations = [];     // refused permission requests — the sandbox was sized too small”; :2501: “Granting here would let Codex step outside the sandbox the caller chose”; :2506: “an escalation is evidence of FAILURE”; :2509: “escalations.push({ method: msg.method, detail, thread: owner, subagent: foreign })”; :2510: “send(refusal)”; :3262: “A refused escalation means the task hit the edge of the”; :3263: “sandbox it was given, so the work is very likely incomplete”.",
    "C9 — skills/seat/scripts/driver.mjs:222: “The post-turn exit ladder, first match wins”; :228–266 defines the ordered TIMEOUT, USAGE, TURN_NOT_COMPLETED, INTERACTION, ESCALATED, VERIFY_UNMEASURABLE, VERIFY_FAILED, NO_COMMANDS, NO_ANSWER and SCHEMA predicates; :243: “an approval was refused: the sandbox was sized too small”; :244: “when: (c) => c.escalations.length > 0”; :3269: “for (const rung of LADDER) if (rung.when(ctx)) return rung.code”; :3270: “return EXIT.OK”; :3288: “codeOverride keeps a rung the ladder cannot reach on its own”; :3328: “const code = codeOverride ?? decideExitCode(ev, verifySkipped)”.",
    "C10 — skills/seat/scripts/driver.mjs:3062: “Every root command item, whatever its verdict”; :3063: “a declined one is already a higher rung”; :3064: “const commandsRan = commands.length”; :3066: “c.status === \"completed\" && c.exitCode === 0”; :3069: “c.status === \"failed\" || c.status === \"declined\"”; :3074: “`declined` is an approval refusal whatever the”; :3075: “code says”; :267: “No rung for a failed command”; :269: “commandsBlocked, fileChangesFailed and commandsProbeNegative stay in the report”; :3096: “const expected = opts.expectRe ? ran.filter(matchesExpectation) : ran”.",
    "C11 — skills/seat/scripts/driver.mjs:2473: “Attribution for REQUESTS must fail closed, and cannot demand ids the schema does not always carry”; :2475: “a request counts as ours unless it positively proves it belongs to another thread”; :2479: “const foreign = rootThreadId !== null && owner != null && owner !== rootThreadId”; :2504: “Recorded whichever thread asked”; :2429–2431 requires the root thread ID and an owned turn ID for success attribution; :2558: “Root-only by the caller's filter, because evidence of success has to be strict”; evals/protocol.test.mjs:222: “evidence of FAILURE must be inclusive even though evidence of SUCCESS is root-only”.",
    "C12 — skills/seat/scripts/driver.mjs:3761: “effectiveSandbox = thread.sandbox ?? null”; :3345: “sandbox: effectiveSandbox”; :3348: “writableRootsRequested: roots, network: Boolean(opts.network)”; :2508: “String(msg.params?.command ?? msg.params?.reason ?? msg.params?.message ?? \"\").slice(0, 200)”; :2509 records only method, detail, thread and subagent; :3385: “escalations, interactions”; :2915 checks “session_meta”; :2916 checks “id !== threadId”; skills/seat/references/environment-and-internals.md:143: “verifies that its opening `session_meta` record names the reported thread”.",
    "C13 — skills/seat/SKILL.md:32: “The task's exit notification is the seat's completion, and `<REPORT>` is what to read then”; skills/orchestrate/SKILL.md:104: “The task's exit notification is when you read that report”; :105: “Wait on every seat you launch in the background, Claude or Codex, with `TaskOutput(<task_id>, block: true, timeout: 600000)`, again while the task still runs”; skills/seat/scripts/driver.mjs:895: “a coordinator reads the report after the”; :896: “task's exit notification”.",
    "C14 — skills/seat/scripts/driver.mjs:66: “DEFAULT_IDLE_TIMEOUT_S: 900”; :99: “CUT_GRACE_MAX_MS: 10000”; :2447: “cutTurn(\"timedOut\", \"idle\"”; :3035: “cutGraceTimer = setTimeout(() => finish(reason), grace)”; :3114 selects only retained answer deltas for partial text; :1811: “if (!turnDone) res.worktreePreserved”; :2271: “killGroup(\"SIGTERM\")”; :2274: “killGroup(\"SIGKILL\")”; :2301: “child.stdin.end()”; :2302: “await quiesceGroup()”; :2304: “releaseLock()”.",
    "C15 — skills/seat/scripts/driver.mjs:2432: “The idle guard accepts activity on the root and its subagent threads”; :2442: “lastEventAtMs = Date.now()”; :2444: “if (idleTimer) clearTimeout(idleTimer)”; :2626: “if (msg.method && onRootThread(p)) touchIdle()”; :2571: “Counted on root”; :2572: “commands only”; :415: “0 disables”; evals/protocol.test.mjs:476: “every event it then sends under its own threadId ... rearms the guard”.",
    "C16 — skills/seat/scripts/driver.mjs:2213: “proc.stdout.on(\"data\", (chunk) =>”; :2219: “dispatch(line)”; :2445: “idleTimer = setTimeout”; :3849: “process.on(sig, () =>”; skills/seat/references/environment-and-internals.md:133: “`SIGKILL` of the driver can strand descendants”; skills/seat/scripts/driver.mjs:3872: “SIGKILL — which takes the report with it”. The synchronous-wait sequence is conditional on that implementation choice.",
    "C17 — skills/seat/scripts/driver.mjs:2112: “if (opts.level !== \"read\") acquireLock(cwd)”; :1428: “if (holderAlive(held)) fail(EXIT.BUSY”; :1429: “is in use by codex-delegate pid”; :2291: “The lock is released only AFTER that wait”; skills/orchestrate/SKILL.md:129: “read `error` and the stderr file, wait for the holder, then run again”; skills/seat/references/parity.md:106: “read seats take no lock and may share one”.",
    "C18 — skills/seat/references/parity.md:102: “Each delegation has its own app-server”; :106: “read seats take no lock and may share one”; schema-0.153.4/ServerRequest.json:356: “multiple callbacks can belong to one parent `itemId`, so `approvalId` is a distinct opaque callback id”; skills/seat/scripts/driver.mjs:899: “it must not exist yet”; :900: “another run's report is two seats' evidence in one file”; :902: “the no-clobber rule survives the whole run”.",
    "C19 — skills/seat/SKILL.md:88: “write only `$TMPDIR`”; skills/seat/references/parity.md:187: “The destination is deliberately not `$TMPDIR`: that is the read level's one writable root, so the very”; :188: “seat being shown the images could edit them”; skills/orchestrate/SKILL.md:30: “Never run `mkdir`, Write or a shell redirect under that data directory yourself, because a headless session refuses each of them as a sensitive file with no prompt anyone can answer”.",
    "C20 — skills/seat/scripts/driver.mjs:566: “a newline IS the field separator”; :568: “the wrapper cannot tell an”; :569: “injected line from one it meant to write”; :571–572: “VERIFY runs an unsandboxed /bin/sh with the caller's own rights, so from a seat file it needs ... --allow-seat-verify on the COMMAND LINE”; skills/seat/references/incidents.md:149: “`EXPECT: x\\nVERIFY: touch /tmp/pwned` became two fields and executed the verifier”; skills/seat/SKILL.md:93: “Never translate a refusal into broader rights.”",
    "C21 — skills/seat/scripts/driver.mjs:2622 dispatches every server request through handleServerRequest without checking terminal state; :2471 defines its direct reply closure as “conn.send({ jsonrpc: \"2.0\", id: msg.id, result })”; :3032 sets “pendingCut”; :3292 sets “settled = true”; :3279 refuses steering when “settled || pendingCut”; :2692: “if (settled) return”. These are existing lifecycle boundaries a delayed approval callback would have to honor.",
    "C22 — skills/seat/scripts/driver.mjs:2708: “Retry a transient failure once only before any observable work, including tool calls and subagents”; :2709: “to avoid repeating side effects”; :2712–2713 checks only commands, fileChanges, messages, otherItems and subagentThreads; :2671 records command evidence only on “item/completed”; :2823: “requestFn(\"turn/start\", lastTurnParams)”. Approval evidence is absent from the replay guard.",
    "C23 — skills/seat/scripts/driver.mjs:3110: “const answerPath = persistAnswer(fullAnswer)”; :3443: “answer, answerPath, answerTruncated”; :3468: “const durable = publishReport(out)”; skills/seat/SKILL.md:150: “Any other non-zero is a gate verdict on the run; read the answer before deciding what to do”; :188: “Read a non-zero result's answer; the exit judges evidence, not whether the answer exists”; skills/seat/references/incidents.md:196–197: “an interrupt discards the in-flight model message. Agent-message deltas now preserve `answerPartial`, and final answers are persisted immediately.”",
    "C24 — skills/seat/scripts/driver.mjs:2122–2123: “if (allowed && !allowed.includes(opts.webSearch))” then “fail(EXIT.USAGE” because the requested mode “is not permitted by this device's managed policy”; :2806: “usageLimitExceeded and rateLimitExceeded ... are deliberately absent”; :2807: “a quota window does not clear in ten seconds”; :2127: “[\"web_search\", opts.webSearch ?? \"disabled\"]”; :3526–3528 selects instructions from “opts.webSearch” and otherwise says “Use the local shell and filesystem only. Do not use web search; cite files you actually read”; README.md:14: “get a finished, verifiable answer back”. The enumerated turn-quota handling does not establish handling of the incident’s search HTTP 429s.",
    "C25 — evals/protocol.test.mjs:10: “node evals/protocol.test.mjs”; :4: “Every row here drives evals/fake-app-server.mjs through a scenario”; :66 expects EXIT.ESCALATED for a refused approval; :210 expects EXIT.TRANSPORT for profile-networked; :221 expects EXIT.ESCALATED for escalated-subagent; :466 expects EXIT.OK while child activity continues; :723 checks the refused-approval ladder rung; :780: “process.exit(summarize(failed, CASES.length + FLOWS.length))”. Executed this turn; observed final output “all 139 passed” and process exit 0."
  ],
  "artifacts": [],
  "open": [
    "The incident’s request frames, report, answerPath and rollout were not supplied. I could not determine whether work was never generated, lost during delivery, or discarded by the coordinator. Those artifacts plus the caller’s handling of exit 6 would settle it; the current documented behavior retains non-zero answers (skills/seat/SKILL.md:188).",
    "I could not establish the runtime scope of a network-related acceptance on codex 0.153.4. Supply the full request, exact response and local enforcement measurements: the intended host works, a second host and forbidden filesystem writes fail, and grants expire across completion/resume. The schema’s optional network context does not prove those properties (schema-0.153.4/ServerRequest.json:410).",
    "D3 has no implementation to inspect. The blocking-loop, mailbox collision, forged-answer and late-decision sequences are counterexamples allowed by the underspecified design, not reproduced defects in a proposed patch. A concrete async lifecycle, protected submission mechanism, request identities and concurrency/cancellation tests would settle them.",
    "The repository does not establish the claimed native-Claude network parity strongly enough to justify changing this contract. A dated measurement of native read-subagent tool access and effective egress would settle that factual premise; it would still leave the documented opt-in migration to address (README.md:172; skills/seat/references/parity.md:3).",
    "No live web requests, MDM enforcement experiments or proposed reviewer tests were run. The 139 passing tests exercise the existing fixture-driven driver only (evals/protocol.test.mjs:4)."
  ]
}