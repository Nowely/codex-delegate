{
  "status": "done",
  "result": "Codex gpt-6-astra B1: done, I audited the full documentation and comment corpus against the supplied standard and proposed fixes without changing files.\nskills/seat/scripts/driver.mjs:3357: “The receipt is the one artefact no wrapper can fabricate” overstates a metadata check; matching session metadata is not authenticated provenance.\nskills/seat/scripts/driver.mjs:1792: “the harvest now holds every byte the force could destroy” overlooks ignored files deliberately excluded from the harvest; name that exclusion.\nskills/seat/scripts/driver.mjs:1367: “a stalled live owner keeps its marker” omits the one-hour age override; document that a live owner can lose an old marker.\nskills/seat/scripts/driver.mjs:901: “the anchor is the PASSWD entry” contradicts the environment-selected state root; delete the obsolete explanation.\nskills/seat/scripts/driver.mjs:51–52: “the full answer is always … written to disk” overlooks write failures and the shorter generation requested by --brief; describe both limits.\nskills/seat/scripts/driver.mjs:3236: “Skipped on a timeout only” omits other incomplete turns and exhausted verification budgets; state the actual conditions.\nskills/seat/references/result-gates.md:66: “The shell never ran the command” for 126/127 mistakes a classification for proof; programs can run and return either status.\nREADME.md:96: “The `mkdir -p` is not decoration” and evals/README.md:159: “worse than no test” are removable rhetoric; preserve the constraints.\nProposed final block: rewrites the history, repetition and documentation rules; adds explicit claim limits and coverage of human-readable strings.\n## Comments and prose\nDefault: no comment. Keep one only for a contract, constraint, or reason the code cannot state. Otherwise delete it; do not refactor code to justify it.\nCut, in this order:\n- Rhetoric: emphasis, metaphor, and comparisons used to insist. Keep the constraint, drop the case for it.\n- Change narration from operating instructions and comments. Put release history in CHANGELOG. Keep dated incident evidence and compatibility history that explain a current constraint, linked from its use.\n- Local repetition and restatements of adjacent names or tests. Keep a constraint at each independently read decision point.\n- Abandoned commented-out code. Keep labeled usage examples.\nJudge meaning, not keywords. Preserve identifiers, platform constants, budgets, and cited measurements with their versions, dates, and limits.\nVerify factual claims against implementation and callers, including failures, opt-ins, and exceptions. Say what a check establishes; do not turn an inference into a guarantee. Mark what you cannot verify.\nDocumentation also earns its place through orientation, instructions, or examples. Judge historical claims against the version they record.\nApply these cuts and factual checks to all documentation and to human-readable help and test-rationale strings.\nThe supplied standard does not assess whether the model-allocation policy is useful: skills/orchestrate/SKILL.md:60 can require “Never implementation” without failing this lens.",
  "evidence": [
    "Unit: one file’s writing—each Markdown document, or the collected comments of one .mjs source. Examined 32 units; 32 failed at least one supplied rule: 13/13 Markdown documents and 19/19 source-comment corpora. These are file counts, not a claim that every comment failed. Located examples supporting every failed unit follow.",
    "Coverage: read all 2,764 lines of the 13 Markdown documents. Extracted and inspected 1,199 contiguous comment groups across 19 .mjs files containing 15,175 source lines. Grouping joined adjacent // comments separated only by whitespace; hashbangs remained attached to their header groups. Code inspection was targeted to factual claims and reported contradictions, not a review of every executable line.",
    "Commands: rg --files for the corpus and AGENTS.md discovery; python3 -B for file/line inventories; nl -ba and sed for numbered reads; rg -n for implementations and references. Used Node’s bundled Acorn parser in memory to distinguish comments from strings. No applicable AGENTS.md was found in the repository or its checked ancestors. No network requests or web search.",
    "Runtime verification: ran 3 read-only probes on Node v24.11.0. An undefined child-process environment property was absent in the child (has:false, value:null). /bin/sh printed 'ran' before returning 126, and printed 'ran' before returning 127. These were probes, not repository test-suite runs.",
    "Source verification: ran 8 distinct source-extracted branch scenarios in Node vm, with filesystem operations mocked in memory: state-root selection; header without SEAT; failed answer persistence; receipt metadata acceptance; verifier skip after failed turn; verifier budget exhaustion; final-answer selection across corrective turns; reclaiming an old marker with a live owner. All expected observations were confirmed. There were 9 executions because the marker scenario was repeated using the actual 3,600,000 ms production constant after an initial injected constant.",
    "Ran 0 repository test suites: they create scratch files and other state. Created no audit files, patches, or temporary directory. The result field was checked in memory: 22 lines.",
    "False guarantee—skills/seat/scripts/driver.mjs:3357, “The receipt is the one artefact no wrapper can fabricate”; repeated at :2900, “the receipt a wrapper cannot forge.” findRollout at :2917 accepts an environment-selected search root; :2942–2947 parses the first record and marks it verified when type and thread ID match. The in-memory probe supplied invented matching metadata and received verified:true, with originator and provider null. Fix: “Checks that the opening session_meta names this thread; does not authenticate the record or report.”",
    "False guarantee—skills/seat/scripts/driver.mjs:1791–1792, “--force included, because the harvest now holds every byte the force could destroy.” The archive input at :1852 uses ls-files --others --exclude-standard; :1866–1870 only counts/samples ignored files; :1887 removes the tree with --force. Fix: specify tracked changes and non-ignored untracked files, and explicitly state that ignored files can be discarded.",
    "False constraint—skills/seat/scripts/driver.mjs:1366–1367, “Owner liveness decides abandonment … a stalled live owner keeps its marker.” The condition at :1381 is dead owner OR age above RECLAIM_BACKSTOP_MS; :110 sets that to 3,600,000 ms. A source-extracted probe with holderAlive returning true and an older marker reached its removal. Fix: state both conditions and the live-owner exception; retain the budget.",
    "Obsolete mechanism—skills/seat/scripts/driver.mjs:901, “the anchor is the PASSWD entry, not $HOME.” stateDir at :997–1006 chooses CODEX_DELEGATE_STATE_DIR, then CLAUDE_PLUGIN_DATA, and otherwise refuses; lockDir at :1008 uses that result. The branch probe selected the explicit environment path. Fix: delete the stale paragraph at :897–904; the current stateDir contract already explains the scope of mutual exclusion.",
    "False guarantee—skills/seat/scripts/driver.mjs:51–52, “the full answer is always … written to disk, so capping what comes back inline costs nothing but a second read.” persistAnswer at :2968–2977 returns null on failure, confirmed by the branch probe. developerInstructions at :3579–3584 also asks for a short answer and, conditionally, details in separate files. Fix: “--brief requests a short answer and clips the inline copy; persistence of generated text is best effort.” Keep the 20-line and 4,000-byte limits.",
    "False skip contract—skills/seat/scripts/driver.mjs:3236, “Skipped on a timeout only.” runVerifier at :3243–3244 skips timed-out and every other non-completed turn; :3252 also skips an exhausted budget. Branch probes returned turn-failed and budget-exhausted. Fix: state those conditions and retain the distinct skip reasons.",
    "False header contract—skills/seat/scripts/driver.mjs:183, “the parser refuses a header that does not open with it.” At :647 the ordering refusal applies only if SEAT exists; :654 supplies read level when it is absent. A MODEL-only header was accepted in the branch probe. Fix: “SEAT, if present, must be first.”",
    "Obsolete failure explanation—skills/seat/scripts/driver.mjs:3713, “abort() prints no report”; repeated at :3906–3907, “abort() writes no report at all.” abort at :2393–2395 calls finish when a thread exists and at :2401 calls preTurnReport otherwise. Fix: remove the false reason; describe the retained evidence and transport verdict where that distinction is needed.",
    "Wrong scope—skills/seat/scripts/driver.mjs:3133–3134, “fall back to the last non-blank unphased message only when nothing was phased at all.” currentFinalMsg at :2764–2773 applies that condition separately within each turn, newest first. The branch probe selected a new unphased answer despite an older phased answer. Fix: add “within that turn,” or delete this duplicate explanation and keep the contract at currentFinalMsg.",
    "README.md:96–97, “The `mkdir -p` is not decoration … which is exactly the account this route is written for.” Rhetoric and repetition: :86 already explains why the directory may be absent. Delete the paragraph; retain the mkdir command and its short explanation.",
    "CHANGELOG.md:21–23, “The reason is parity … where a knob and a default compete, the default wins.” The release’s network-default change belongs here; its slogan does not. Delete the slogan and retain the changed behavior, compatibility implications, and dated measurements.",
    "RELEASING.md:8, “set it in the four places `evals/package.test.mjs` compares.” There are five current locations: plugin.json, three SKILL.md files, and driver VERSION. Verified against the directory-derived skillPages and seen object in evals/package.test.mjs:26–45 and a read-only inventory that printed 5. Fix: remove “four” and retain the enumerated sources.",
    "RELEASING.md:36–37, “For 0.11.0 it is mandatory.” This historical release qualifier sits in the ongoing checklist. Fix: state the current live-gate requirement without that version qualifier; keep the historical requirement in its release entry.",
    "evals/README.md:29, “the last one was wrong twice in two days,” is change narration attached to the useful rule that suite output owns counts. Delete that clause. At :159–160, “A fixture that diverges from the schema is worse than no test, because it manufactures confidence,” replace the comparison with the requirement that fixture output conform to the pinned schema.",
    "skills/cleanup/SKILL.md:15–16, “never retell the listing in your own words,” and :20–21, “a listing block is the one thing shown exactly as the command printed it,” repeat the same constraint within five lines. Under the supplied rule, consolidate the preamble into one instruction to read JSON privately and show the listing unchanged; retain the operational instruction at the listing step.",
    "skills/orchestrate/SKILL.md:106, “measured 2026-09-08: a seat's exit at minute 9 surfaced only when the user asked, while its sibling ran 18 minutes.” This incident narrative interrupts the mechanism instruction. Keep the current Workflow notification limitation and routing rule here; move the dated observation intact to the incident reference.",
    "skills/seat/SKILL.md:53, “Write a prompt you were handed VERBATIM: not a quote, not a `$`, not a header line it has.” Keep the byte-preservation constraint; replace the insistence with “Copy the supplied prompt byte for byte, without additions or substitutions.”",
    "skills/seat/references/adversarial-review.md:29–30, “confirm each finding is … grounded at a concrete code location, plausible in a real failure scenario, and actionable,” repeats :23–28’s actionable, located, evidence-based finding requirements. The literal repetition rule would consolidate these. Its separate pre-return function is a reason to consider the decision-point exception in the proposed block.",
    "skills/seat/references/environment-and-internals.md:3–4, “Moved out of `SKILL.md` because none of it is needed at the moment of deciding whether and how to delegate.” This is the document’s editing history. Delete it; :6–7 already states the reference’s purpose.",
    "skills/seat/references/incidents.md:3–4, “Each line is evidence, not folklore: if a rule ever looks like ceremony, this is what it cost to learn.” Rhetoric adds no evidence or constraint. Replace with “Measured failures behind the skill’s operating constraints”; preserve the actual incident records.",
    "skills/seat/references/parity.md:162, “it supports exactly ONE BrowserContext,” and :168, “refactored into a FUNCTION.” Lowercase the emphatic ordinary words. Preserve the one-context constraint, identifier BrowserContext, conditions, versions, and measurements.",
    "skills/seat/references/result-gates.md:66, “127 / 126 … The shell never ran the command”; also skills/seat/scripts/driver.mjs:3260. The implementation at driver.mjs:3266–3268 classifies these statuses as unmeasured but cannot establish their cause. Both shell probes executed printf before returning those statuses. Fix: “126/127 are treated as unmeasured; they commonly indicate execution or lookup failures, but programs can return them themselves.”",
    "skills/seat/references/result-gates.md:3–4, “Extracted from SKILL.md … Read it when the pointer in SKILL.md sends you here.” The hidden HTML comment records relocation and restates navigation. Delete it.",
    "skills/seat/references/why-not-the-plugin.md:42, “a decorrelated opinion bought and a crippled one delivered,” and :125, “a fix that silently reverts is worse than none.” Delete the metaphors and comparison; retain the stated test limitation and the fact that plugin updates overwrite cache patches.",
    "skills/seat/scripts/cleanup.mjs:915 and :918 repeat exactly: “A row may stand for many directories, so a count of rows is never presented as a count of things.” Delete one copy.",
    "skills/seat/scripts/attach-pasted.mjs:305, “mkdir's mode is umask-masked; the chmod is not decoration.” The umask explanation earns its place; “not decoration” does not. Keep the first clause and delete the second.",
    "evals/agent-contract.test.mjs:89–90, “VERIFY is the one field … which this relay never passes.” The file’s own header at :6 says there is no relay; the current check at :95–97 validates the coordinator page’s refusal wording. Fix: name the coordinator recipe, or simply retain that VERIFY requires command-line authorization.",
    "evals/attach-pasted.test.mjs:34, “The machine records that share type \"user\" and are NOT human turns.” “NOT” is emphatic prose, not an identifier. Lowercase it; retain the distinction that explains the fixtures.",
    "evals/cleanup.test.mjs:775–780, “Built beside the original … measured 2026-09-10, both Linux jobs rebuilt this tree with the SAME dev:ino … the case measured nothing it claimed.” Keep the present constraint: allocate the replacement while the original retains its inode. Move the failed-CI narrative to release/incident history and remove the insistence; preserve the measured filesystem qualification.",
    "evals/cli.test.mjs:528, “the temp name the rename published from is gone.” publishReport in driver.mjs:950–951 writes a temporary file and publishes by linkSync, then removes the temporary name at :960–961. Fix: “The temporary publication name is gone,” or delete the comment if the adjacent assertion suffices.",
    "evals/conformance.test.mjs:9, “looked at\\\".” This orphaned fragment carries no contract, constraint, or reason. Delete it.",
    "evals/fake-app-server.mjs:922–923, “Both must reach the report, and the failed patch must reach the exit ladder.” The current LADDER in driver.mjs:232–275 has no failed-patch rung; evals/protocol.test.mjs:231 expects EXIT.OK for this scenario. This was verified by reading, not executing that test. Fix: retain the report requirement and remove the retired verdict requirement.",
    "evals/fidelity.test.mjs:11–12, “It only ever performs … the handshake — no turn is started and no model is called.” The opt-in path at :678 calls liveTurns; :568–571 launches a real driver turn. Fix: distinguish default handshake checks from CODEX_DELEGATE_LIVE_TURN=1. Keep that opt-in visible in the header.",
    "evals/lib/harness.mjs:52, “`undefined` in a spawn env is stringified to \"undefined\".” The runtime probe observed omission instead. Lines :56–59 explicitly remove such entries before spawn, but the reason given is false on the measured runtime. Delete the false explanation without changing the code.",
    "evals/lib/scenarios.mjs:112–114, “a scenario name a suite misspells would reach the fixture's default branch, and a case that measures nothing looks exactly like a case that passes.” The fixture now rejects unknown inventory names at evals/fake-app-server.mjs:99–101. Fix: describe this helper’s early, suite-level diagnostic, without claiming the fixture silently succeeds.",
    "evals/lock.test.mjs:366–367, “a decoy HOME must leave NOTHING behind.” Lowercase “nothing”; retain HOME as an identifier and retain the no-write constraint. This is a minor emphasis violation, not a disputed platform budget.",
    "evals/orchestrate-live.test.mjs:293–294, “the driver publishes those files and the coordinator writes there at all.” The sentence reverses the constraint: :304 says “only the driver writes there,” and :303–312 rejects extra entries. Fix: “Only the driver publishes files there.”",
    "evals/orchestrate.test.mjs:298, “the agent this release deleted, offered again by name, is a seat that never runs.” Remove “this release deleted.” Keep the current constraint that Codex seats use the driver route; the adjacent check already names the prohibited agent.",
    "evals/package.test.mjs:7–8, “Nothing asserted what must be in it, and nothing compared the places the version is written.” This describes the situation before the suite existed. Delete it; retain the payload and version-agreement purpose.",
    "evals/protocol.test.mjs:711–714, “most of them used to read module state … could be exercised end to end and no other way.” Remove the refactoring history. Keep the current contract that each imported ladder predicate can be evaluated with a constructed context.",
    "evals/run-all.mjs:55–56, “process.exit(null) exits 0: a killed suite used to end the run green.” Keep the current null-status hazard; delete the past failure narrative.",
    "evals/worktree.test.mjs:33, “--level write, so the worktree is actually created.” The helper at :37 supplies write level, but driver.mjs:2094–2099 creates a worktree only when opts.worktree is set. Fix: “Cases request worktree creation through --worktree in args,” or delete the misleading causal comment.",
    "No abandoned commented-out implementation was identified. Commented commands such as evals/run-all.mjs:4, “node evals/run-all.mjs (or: npm test),” are usage examples, not dormant implementation."
  ],
  "artifacts": [],
  "open": [
    "Historical and external claims were not remeasured: for example, skills/seat/references/why-not-the-plugin.md:3–6 dates its plugin-source and live-protocol observations. I could verify local implementations and contradictions, but could not independently establish every claimed historical measurement or current external behavior. No live model sessions, Linux runs, or network fetches were performed.",
    "The history rule fights necessary evidence qualification. skills/seat/references/parity.md:3–5 says the figures “were not re-measured for the 0.153.4 pin” and should be treated as “an order of magnitude.” Removing that as history while retaining the numbers would make the documentation less truthful. Preserve dates, versions, and limitations together.",
    "The same rule fights corrected incident attribution. skills/seat/references/incidents.md:64–68 says “that attribution is wrong” and records “zero created by this driver.” That correction prevents the repository from blaming its driver for another tool’s leaked worktrees. Keep the evidence; remove the surrounding rhetoric rather than deleting the incident.",
    "The blanket repetition rule needs a decision-point exception. skills/seat/references/adversarial-review.md:29 begins “Before returning,” giving repeated finding criteria a distinct final-check role. File failures above use the supplied rule literally; the proposed rule permits deliberate repetition where independently read instructions need it.",
    "“Default: no comment” does not supply a sufficient positive rule for documentation. README.md:63–65’s marketplace/install commands earn their place as instructions even without a reason code cannot state. The proposal explicitly admits orientation, instructions, and examples.",
    "The source boundary needs clarification: evals/protocol.test.mjs:114 contains “IMAGES FIRST” and “on this machine” inside a why string. A comment-only audit leaves this equivalent writing untouched. The proposed block explicitly includes human-readable help and test-rationale strings; those strings were not additional counting units."
  ]
}