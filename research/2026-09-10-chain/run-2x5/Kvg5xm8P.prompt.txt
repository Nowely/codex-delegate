SEAT: read /Users/ruliny/arcadia/data-ui/cloud-components
MODEL: gpt-6-astra
OUTPUT_SCHEMA: /var/folders/mf/9v804k_57lq30kwtlr7468xr0000gn/T/br-review.XDDuwFFp/schema.json
TASK:
Ты — место V1, рефьютер. Пять ревьюеров разобрали ветку CLOUDFRONT-62071.adaptive-baremetal-config-cards в
/Users/ruliny/arcadia/data-ui/cloud-components и сдали находки. Твоя работа — не искать новое, а ОПРОВЕРГАТЬ
сданное. Ты не писал этот код и не находил эти находки: у тебя нет причин их щадить.

Сводный список находок: /var/folders/mf/9v804k_57lq30kwtlr7468xr0000gn/T/br-review.XDDuwFFp/findings.md
Полный диф ветки: /var/folders/mf/9v804k_57lq30kwtlr7468xr0000gn/T/br-review.XDDuwFFp/branch.diff
VCS здесь arc, не git: команды git не работают. Merge-base с trunk = ca9489edac04d749ad408d2e56b2fbe2eb4ddfcd.

ЖЁСТКОЕ ОГРАНИЧЕНИЕ ПО ФАЙЛОВОЙ СИСТЕМЕ. /Users/ruliny/arcadia — сетевой FUSE-mount. Рекурсивные обходы
(grep -r, rg, ag, find, ls -R) по путям выше третьего уровня от /Users/ruliny/arcadia ЗАПРЕЩЕНЫ: они тянут
файлы по сети и приводят к бану аккаунта. Разрешено: адресное чтение файлов по полному пути и bounded-grep
внутри /Users/ruliny/arcadia/data-ui/cloud-components/src,
/Users/ruliny/arcadia/data-ui/cloud-components/node_modules/@gravity-ui (локальный каталог, не mount),
/Users/ruliny/arcadia/data-ui/cloud-console/src/ui/shared и /Users/ruliny/arcadia/data-ui/cloud-console/.memory-bank.
Больше никуда не ходи. Сети нет.

ПРАВИЛО ВЕРДИКТА: по умолчанию находка ОПРОВЕРГНУТА. Она выживает, только если ты сам, своими командами,
воспроизвёл её основание. «Похоже на правду» = опровергнута. «Не смог проверить» = опровергнута, и ты пишешь,
чего именно не хватило.

Разбери поимённо эти находки, в этом порядке приоритета. Первые четыре — несущие, на них держится половина
предлагаемых правок; если хватит времени только на них, сделай их и честно скажи, что остальные не разобраны.

1. A2 — «tabsRef не спасает от размонтирования, при переключении таба React размонтирует оба поддерева и
   сбрасывает initialized вплоть до возврата к статическому defaultValue».
   Прочти src/hooks/useSegmentedTabs.tsx целиком. Проверь по семантике React: действительно ли смена
   идентичности функции-типа компонента между рендерами родителя приводит к размонтированию поддерева,
   и действительно ли зависимость [activeTab] у useCallback меняет эту идентичность именно при переключении
   таба. Отдельно проверь ВТОРУЮ половину утверждения — что после размонтирования эффект defaultValue
   способен вернуть таб назад. Если вторая половина не следует из первой, скажи это: находка может быть
   верна наполовину. Если можешь, подтверди вычислением, а не рассуждением.

2. C1 — «getMainWidth немонотонен, есть остров wide на 595-715px».
   Это чистые функции. Не рассуждай — выполни их. Прочти src/components/baremetal/shared/lib/layout.ts,
   перепиши формулы в отдельный node-скрипт в своей временной папке и прогони ширины 200..1400 с шагом 1
   для columnGap 40 и 22. Приведи фактические точки переходов числами. Если острова нет — находка
   опровергнута. Если есть — скажи, наблюдаем ли он реально: то есть достижимы ли эти ширины страницы
   у настоящих потребителей, и что при этом видит пользователь.

3. D1 — «visible в ResponsivePopover остаётся true после перехода sheet→popover, при возврате в mobile
   шторка откроется сама».
   Прочти src/components/baremetal/shared/components/ResponsivePopover/ResponsivePopover.tsx и
   src/hooks/useBooleanState.ts. Проверь: при `asSheet === false` компонент возвращает ветку Popover,
   Sheet не рендерится — но состояние хука живёт в том же компоненте и не сбрасывается. Действительно ли
   сценарий воспроизводим (нужно: открыть шторку, расширить окно, сузить обратно), и попадает ли он в
   реальный пользовательский путь или только в ресайз.

4. B1 — «useCollapseChildren неприменим к SegmentedRadioGroup, а TabList contentOverflow=collapse решает
   задачу коротких подписей».
   Проверь ОБЕ половины по declarations в node_modules/@gravity-ui/uikit (назови версию). Отдельно проверь,
   что предложенная замена сохраняет то, ради чего взят SegmentedRadioGroup: внешний вид сегментированного
   переключателя и `width="max"`. Если TabList даёт другой визуальный компонент, то это не «упрощение»,
   а смена дизайна, и находку надо переклассифицировать.

5. C3 — «ранний return в ConfigurationPageLayout.tsx:34-36 идёт мимо провайдера, потребитель с одним child
   молча получает wide». Сдавшее место само пометило это гипотезой. Проверь по коду
   ConfigurationPageLayout.tsx и PresetConfigurationRoot.tsx, достижимо ли это состояние изнутри библиотеки.

6. C4 — «наблюдателя два, и enabled не отписывает useResizeObserver, а только глушит колбэк».
   Проверь по реализации node_modules/@gravity-ui/uikit/build/esm/hooks/useResizeObserver и по двум
   вызовам в ветке. Есть ли реальная цена у второй подписки или это косметика.

7. E1 — «display: contents ВЫНУЖДЕН, areas и subgrid его не устранят». Это опровержение, сданное как
   находка. Опровергни опровержение: попробуй найти раскладку, при которой обёртки не нужны, читая
   ConfigurationCardLayout.tsx и ConfigurationCard.scss. Если не нашёл — подтверди, что вынужден.

8. F4 — «все три новых visual-теста не делают ни одного скриншота, при том что 31 из 34 остальных делают».
   Пересчитай сам, своей командой, и скажи, дефект это или осознанная конвенция для тестов на геометрию.

9. C6 — «DEFAULT_LAYOUT маскирует отсутствие провайдера, надо useAssertedContext».
   Проверь, действительно ли useAssertedContext бросает в production (прочти src/hooks/useAssertedContext.ts),
   и не сломает ли замена сторибук и изолированный рендер экспортируемых компонентов. Учти: cloud-components —
   публикуемая библиотека.

10. G1 — «sidebarSize compact полностью анмаунтит фильтры, состояние теряется». Проверь по коду, что
    происходит с панелью, и правда ли состояние теряется, а не сохраняется выше по дереву.

CHECK:
- Для КАЖДОЙ находки верни ровно один из вердиктов: ПОДТВЕРЖДЕНА / ОПРОВЕРГНУТА / ВЕРНА ЧАСТИЧНО.
  Рядом — команда или вычисление, которым ты это установил. Вердикт без своей команды не принимается;
  ссылка на то, что «так написало предыдущее место», не является основанием.
- Где находка верна, но названа не тем словом (баг vs смена дизайна vs косметика) — переклассифицируй.
- Не предлагай новых правок и не ищи новых находок. Твоя единственная работа — вердикты.

RETURN:
Пять полей по схеме из OUTPUT_SCHEMA.
Поле result: не больше 30 строк. Первая строка — одно самостоятельное предложение вида
«Место V1, gpt-6-astra: <статус>, <сколько находок подтвердил, сколько опроверг>». Дальше по одной строке
на находку в формате: <id> — <ВЕРДИКТ> — <чем установлено> — <переклассификация, если есть>.
Пиши по-русски.
Поле evidence: команды и вычисления с числами.
Поле open: что не смог проверить и почему; какие находки не успел разобрать.
