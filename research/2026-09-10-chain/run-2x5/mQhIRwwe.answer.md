{"status":"done","result":"Codex gpt-6-astra C1: done; I reviewed the repository’s documentation and code comments and proposed corrections for misleading guarantees, stale instructions and unclear prose.\nI examined 32 file units; 15 failed: 9 documentation files and 6 source-comment files. Each failed file has a quoted defect below.\n1. Potential loss is described as lossless: skills/seat/scripts/driver.mjs:1791–1792 says removal is safe “because the harvest now holds every byte the force could destroy.” The archive excludes ignored files. Replace with: “The harvest preserves tracked changes and non-ignored untracked files. Forced removal deletes ignored files without archiving them.”\n2. The worktree instructions can make someone verify the wrong code. skills/orchestrate/SKILL.md:48–49 says “the commit or stash that feeds the worktree is a live-tree commit”; skills/seat/SKILL.md:130 says “Commit or stash relevant work first”; skills/seat/references/parity.md:41 says “commit or stash relevant WIP first.” Replace these prescriptions, including driver.mjs:311–312, with: “A new worktree starts at HEAD. To include your current edits, commit them first with approval, or run the check in the live checkout. Stashing does not put those edits into the new worktree.”\n3. Receipt promises exceed verification. README.md:5–6 promises “Every completed turn leaves a receipt — a rollout the driver locates, opens and checks”; driver.mjs:2900 calls it “the receipt a wrapper cannot forge.” Lookup can return null, and validation checks a session record’s type and ID. Replace with: “The report includes the result of looking up the thread’s session log. A matching session record establishes the recorded thread ID; it does not independently prove task completion or protect against a caller fabricating evidence.”\n4. A stale comment misidentifies the lock boundary. driver.mjs:901 says “the anchor is the PASSWD entry, not $HOME.” The implementation uses an explicitly configured state directory. Replace the block at 897–905 with: “Locks live under the configured state directory and are keyed by directory identity. Runs using different state directories do not exclude each other.”\n5. Cleanup lacks the documented standalone installation route. skills/cleanup/SKILL.md:29 and :59 invoke “${CLAUDE_PLUGIN_ROOT}/skills/seat/scripts/cleanup.mjs”, although README.md:87–94 also installs cleanup as a standalone symlink. Replace that executable path in both recipes with “${CLAUDE_SKILL_DIR}/../seat/scripts/cleanup.mjs”; the documented installation supplies the sibling seat skill.\n6. The fidelity suite’s cost is described inconsistently. evals/fidelity.test.mjs:11–12 says “It only ever performs ... the handshake — no turn is started and no model is called”; evals/README.md:123–124 repeats “No turn is started and no model is called.” Replace both claims with: “By default, fidelity compares handshakes without calling a model. CODEX_DELEGATE_LIVE_TURN=1 additionally runs a real model turn.”\n7. A comment conceals the effect of --brief. driver.mjs:51–52 says it is “not [about] the seat’s thoroughness” and “costs nothing but a second read.” The generated instructions demand a shorter answer. Replace with: “--brief asks the model for at most 20 lines and clips the inline answer to the configured limits. answerPath preserves generated text, not detail omitted during generation.”\n8. Accounting explanations disagree. skills/seat/references/environment-and-internals.md:24 calls total “thread-cumulative across --resume”, then :26 says it is “what a single turn cost”; driver.mjs:2715 calls last “the most recent turn alone”, while the reference at :25 says “API request”. Replace both explanations with: “total is cumulative for the root thread, including earlier turns. last covers the latest API request. The driver forwards these values without calculating an invocation total.”\n9. One release entry preserves opposing designs. CHANGELOG.md:110 says “ONE inventory answers every number”, but :229 says “the inventory is re-run per number.” Replace the latter bullet with: “Selections are matched by kind and name against one fresh inventory before removal begins. Liveness is checked again immediately before each member is removed.”\n10. The README makes readers traverse implementation history before installation. README.md:28–37 opens cleanup with “It removes four kinds” and “only ever reports the other four”, followed by ownership and process-record exceptions. Replace that paragraph with: “/codex-delegate:cleanup lists leftover plugin files and removes the items you approve. See skills/cleanup/SKILL.md for selection rules and limitations.” Move Goal below First run so setup is encountered earlier.\n11. The central copying instruction is grammatically incomplete. skills/seat/SKILL.md:53 says “Write a prompt you were handed VERBATIM: not a quote, not a `$`, not a header line it has”. Replace with: “Copy a supplied prompt byte for byte, including quotes, dollar signs and header lines. Do not add, remove or rewrite text.”\n12. A release instruction has already outgrown its count. RELEASING.md:8 says “the four places”, but the comparison covers five version values. Replace with: “Set the version in .claude-plugin/plugin.json, metadata.version in every skills/*/SKILL.md, and the driver’s VERSION.”\n13. A fixture comment still teaches a retired verdict. evals/fake-app-server.mjs:922–923 says “the failed patch must reach the exit ladder.” Replace with: “The report must include successful changes and the failed patch. A failed patch alone does not change the exit code.”\n14. A test comment names the wrong publication mechanism. evals/cli.test.mjs:528 says “the temp name the rename published from is gone.” Replace with: “The temporary file is removed after hard-link publication.”\n15. A missing negative reverses an instruction. evals/orchestrate-live.test.mjs:293–294 says “the driver publishes those files and the coordinator writes there at all.” Replace with: “Only the driver writes reports here; the coordinator keeps prompts and redirected output under $TMPDIR.”\n16. An abandoned sentence fragment remains in evals/conformance.test.mjs:9: “// looked at\\\".” Delete that line.\nThe recurring writing problem is that repairs accumulated as explanations beside older explanations; reconcile each current contract before shortening it, and keep historical accounts explicitly historical.\nUnaided, I suspect I am weakest at catching missing explanations that only a first-time Claude Code user would notice.","evidence":["Repository: /Users/ruliny/Git/codex-delegate; HEAD ad8cd99a08b944c02b50a40b7111f4dce346e7ad. Commands used: rg --files, rg -n, nl -ba with sed, wc -l, cat, git status --short, git rev-parse HEAD, and an in-memory python3 -B inventory. No network calls, file edits or temporary artifacts.","Coverage: all 13 requested Markdown documents, totaling 2,764 lines; comments in all 19 .mjs files under skills/ and evals. Those source files contain 15,175 lines; implementation sections were read selectively to verify findings. Comment scans included standalone and inline // and /* comments; truncated outputs were reread in smaller ranges.","Unit choice: one Markdown document or one source file’s comments. Instructions and explanations need their surrounding context, and file units avoid arbitrarily splitting long comment blocks. Failed means at least one retained, located defect requiring correction. Counts: 32 examined, 15 failed, 17 with no retained finding; the latter are not certified error-free.","Failed documentation units (9): README.md, CHANGELOG.md, RELEASING.md, evals/README.md, skills/cleanup/SKILL.md, skills/orchestrate/SKILL.md, skills/seat/SKILL.md, skills/seat/references/environment-and-internals.md, skills/seat/references/parity.md. Failed source-comment units (6): skills/seat/scripts/driver.mjs, evals/cli.test.mjs, evals/conformance.test.mjs, evals/fake-app-server.mjs, evals/fidelity.test.mjs, evals/orchestrate-live.test.mjs.","Worktree verification: driver.mjs:1716–1717 invokes git worktree add --detach without a revision for a fresh run; :1740 restores prior harvested work only on resume. Searching its stash references found prose, not a stash-application operation. Archive construction at :1852 uses ls-files --others --exclude-standard; :1866–1870 reports ignored files separately; :1887 force-removes the tree.","Receipt and lock verification: driver.mjs:2917–2955 performs bounded lookup, validates session_meta and its ID, and can return null. The exit ladder at :232–275 contains no receipt requirement; evals/cli.test.mjs:258–260 explicitly expects exit 0 with receiptOk false (assertion read, not executed). stateDir at driver.mjs:995–1008 selects CODEX_DELEGATE_STATE_DIR, then CLAUDE_PLUGIN_DATA; acquireLock at :1410–1412 uses that directory.","Instruction verification: driver.mjs:3579–3584 inserts the --brief generation limit. evals/fidelity.test.mjs:678 invokes liveTurns when its opt-in variable equals 1; :568–571 launches the real driver for that turn. Cleanup-path finding is a static comparison of README.md:87–94, the two skill recipes, and the shipped directory layout; neither installation route was executed.","Accounting verification: driver.mjs:2716–2717 copies the incoming root-thread tokenUsage payload without computing a turn delta. The API-request interpretation of last is recorded in environment-and-internals.md:24–26 and evals/fake-app-server.mjs:246–249; I did not independently remeasure the server’s semantics.","Remaining code checks: cleanup.mjs:1102 creates one inventory before the selection loop, :1133 begins removal, and :999–1001 rechecks member liveness. evals/package.test.mjs:26–27 discovers three skill pages; :39–42 combines their versions with the manifest and driver versions. driver.mjs:951 publishes with linkSync. The file-change case at evals/protocol.test.mjs:231–240 expects exit 0 while asserting the failed patch is reported. evals/orchestrate-live.test.mjs:303–312 rejects files outside the intended report layout.","Tests executed: 0. Suites create filesystem state and were not run under the read-only instruction. Initial and final git status --short output was empty."] ,"artifacts":[],"open":["The proposed standalone cleanup path was checked against the documented layout, not exercised in a fresh Claude Code installation.","Historical measurements, upstream-plugin claims and platform-specific behavior were read as repository claims, not independently reproduced. The live meaning of tokenUsage.last remains dependent on the repository’s recorded observation.","Implementation should update affected literal prose assertions in evals/agent-contract.test.mjs and evals/orchestrate.test.mjs alongside the wording, then run the suites; no test results are claimed here."]}