{
 "bank": "terse-calibration-1",
 "built": "2026-09-11",
 "repeats": 5,
 "frame": "English project documentation in Nowely/agent-skills. Discovery sources are plugins/codex-delegate and research/2026-09-10-chain/chain; plugins/terse and research/2026-09-11-terse-survey are held out, pinned at commit d66a2ef.",
 "engines": [
  "gpt-6-astra",
  "gpt-5.6-sol",
  "gpt-5.6-terra",
  "claude-opus-5",
  "claude-sonnet-5",
  "none-extracted"
 ],
 "items": [
  {
   "id": "astra-tone-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:13-18",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "Release tags now include the plugin name, preventing a sibling's release from being mistaken for this plugin's version. All published tags were renamed. The marketplace address is Nowely/agent-skills: replace the old registration with the new one. The nowely registration name, codex-delegate@nowely identifier and existing data directory remain unchanged, easing migration.",
     "words": 50
    },
    "off": {
     "text": "Release tags now include the plugin name, so each tag identifies one plugin's version within the shared marketplace namespace. All published tags were renamed. The marketplace address is Nowely/agent-skills: replace the old registration with the new one. The registration name remains nowely, the identifier remains codex-delegate@nowely, and the data directory stays in place.",
     "words": 53
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "astra-tone-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:31-35",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "The snapshot test now builds a replacement beside the original directory before renaming it over the original. Keeping both directories allocated prevents an inode collision on any filesystem. This reliably establishes the identity change the test needs; rebuilding in place let ext4 reuse the inode and caused both Linux CI jobs to fail.",
     "words": 53
    },
    "off": {
     "text": "The snapshot test now builds a replacement beside the original directory before renaming it over the original. This prevents inode collisions on any filesystem. Previously, rebuilding in place let ext4 reuse the inode, leaving dev:ino unchanged. The test's identity-change precondition then failed, and both Linux CI jobs reported failures.",
     "words": 49
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "astra-tone-3",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:98-100",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "A single read helper returns either a value or a reason the value could not be obtained. Only ENOENT becomes a value: absence is known, while other errors leave the state unknown. Callers keep the item whenever a reason is returned, providing a consistent safeguard against deletion based on missing information.",
     "words": 51
    },
    "off": {
     "text": "A single read helper returns either a value or a reason the value could not be obtained. ENOENT is the only error converted to a value because it establishes absence. Other errors leave the state unknown. Every caller that receives a reason classifies the item as one to keep.",
     "words": 49
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "astra-tone-4",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:130-133",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "Cleanup verifies every selected number against one inventory before deleting anything, making the result independent of selection order. Previously, a fresh inventory for each number could change the outcome: deleting a run renamed its associated seat, invalidating the seat's selection. Reversing those two numbers deleted both. The shared inventory removes that inconsistency.",
     "words": 52
    },
    "off": {
     "text": "Cleanup verifies every selected number against one inventory before deleting anything. The selection order therefore does not affect the result. Previously, cleanup created a fresh inventory for each number. Deleting a run renamed its associated seat, so that seat's number was refused as changed; selecting the seat first allowed both items to be deleted.",
     "words": 54
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "astra-tone-5",
   "type": "single-factor",
   "source": "plugins/codex-delegate/README.md:41-45",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "Install codex on PATH and authenticate it; codex login status checks the session. Runs reuse credentials through an auth.json symlink from your real ~/.codex. Each report identifies the pinned CLI build as codexVersionPinned, with its protocol reference in schema-<version>/. Running the fidelity suite after upgrades provides a useful compatibility check before trusting results.",
     "words": 53
    },
    "off": {
     "text": "Install codex on PATH and authenticate it; codex login status checks the session. Runs reuse credentials through an auth.json symlink from your real ~/.codex. Each report identifies the pinned CLI build as codexVersionPinned. The corresponding schema-<version>/ directory contains its protocol reference. After upgrading codex, run the fidelity suite before trusting a run.",
     "words": 52
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "astra-tone-6",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:139-141",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "Cleanup checks the directory's dev:ino again at removal, giving the user's selection protection against a replacement made after inventory. If the listed directory has been removed and another now occupies its path, cleanup refuses that replacement. The identity comparison keeps deletion tied to the directory the listing actually measured.",
     "words": 49
    },
    "off": {
     "text": "Cleanup checks the directory's dev:ino again at removal and compares it with the identity recorded by the listing. If the listed directory has been removed and another now occupies its path, cleanup refuses to remove the replacement. The comparison occurs after inventory, immediately before the selected directory is removed.",
     "words": 49
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   },
   "repeat": true
  },
  {
   "id": "astra-tone-7",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/incidents.md:219-223",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "The driver now places TMPPREFIX under each run's writable TMPDIR, enabling zsh here-documents within the existing grant. Previously, zsh used /tmp/zsh, where seats could not write. The failure appeared in 15 rollouts between August 31 and September 8, 2026, and was reproduced under the read sandbox. The /bin/sh shell was unaffected.",
     "words": 51
    },
    "off": {
     "text": "The driver now places TMPPREFIX under each run's writable TMPDIR. Previously, zsh here-documents used /tmp/zsh and failed because seats could not write there. The failure appeared in 15 rollouts between August 31 and September 8, 2026, and was reproduced under the read sandbox. The /bin/sh shell did not exhibit the failure.",
     "words": 51
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "astra-tone-8",
   "type": "single-factor",
   "source": "research/2026-09-10-chain/chain/README.md:3-8",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "The plugin makes a Codex second opinion convenient within Claude Code. Claude can delegate analysis, document review, competing designs or implementation, declaring write permissions for each call. The driver returns observed events as a structured JSON report. A successful exit or matching session record alone does not prove that the requested task succeeded.",
     "words": 53
    },
    "off": {
     "text": "The plugin lets Claude Code delegate work to Codex for a second opinion, including analysis, document review, competing designs or implementation. Each call declares write permissions, and the driver returns a structured JSON report of observed events. A successful exit or matching session record alone does not prove that the requested task succeeded.",
     "words": 53
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "astra-metaphor-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:72-75",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "Release 0.12.0 was measured against codex-cli 0.153.4 on macOS. It changes no driver behavior. It adds a third skill and its script, revises user-facing material across all three skills, and renames user-visible names. Upgrading requires replacing the marketplace registration with nowely; running plugin update alone does not complete the migration.",
     "words": 50
    },
    "off": {
     "text": "Release 0.12.0 was measured against codex-cli 0.153.4 on macOS. It changes no driver behavior. It adds a third skill and its script, revises user-facing material across all three skills, and renames user-visible names. Upgrading requires replacing the marketplace registration with nowely; running plugin update alone will not get you there.",
     "words": 50
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "astra-metaphor-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/README.md:99-105",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "The plugin stores state in CLAUDE_PLUGIN_DATA, resolved here to ~/.claude/plugins/data/codex-delegate-nowely/. That directory contains answers, the isolated Codex home, write locks, the worktree ledger and orchestrator run directories. Updates preserve it; uninstall removes it unless --keep-data is passed. The cleanup skill lists its contents and removes the items you select.",
     "words": 49
    },
    "off": {
     "text": "The plugin keeps state under one roof in CLAUDE_PLUGIN_DATA, resolved here to ~/.claude/plugins/data/codex-delegate-nowely/. That directory contains answers, the isolated Codex home, write locks, the worktree ledger and orchestrator run directories. Updates preserve it; uninstall removes it unless --keep-data is passed. The cleanup skill lists its contents and removes the items you select.",
     "words": 52
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "astra-metaphor-3",
   "type": "single-factor",
   "source": "plugins/codex-delegate/README.md:131-135",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "Run suites from the repository or plugin root. Appending ../.. to a symlinked skill path can produce a nonexistent path because Node resolves those segments lexically, although ls follows the link successfully. Resolve the symlink, use the install path announced when the skill loads, or read installPath from installed_plugins.json.",
     "words": 49
    },
    "off": {
     "text": "Run suites from the repository or plugin root. Appending ../.. to a symlinked skill path can lead to a dead end because Node resolves those segments lexically, although ls follows the link successfully. Resolve the symlink, use the install path announced when the skill loads, or read installPath from installed_plugins.json.",
     "words": 50
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "astra-metaphor-4",
   "type": "single-factor",
   "source": "plugins/codex-delegate/README.md:174-178",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "Both permission levels allow network access without a host allowlist. The --no-network flag blocks sandbox traffic; provider web search remains separately controlled by --web-search and may still be enabled. Network access does not expand filesystem write permissions: a read seat writes only TMPDIR, but it can transmit any file it can read.",
     "words": 52
    },
    "off": {
     "text": "Both permission levels allow network access without a host allowlist. The --no-network flag blocks sandbox traffic; provider web search has its own switch in --web-search and may still be enabled. Network access does not expand filesystem write permissions: a read seat writes only TMPDIR, but it can transmit any file it can read.",
     "words": 53
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "astra-metaphor-5",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/SKILL.md:53-56",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "Copy a supplied prompt verbatim, including quotes, dollar signs and header lines; add nothing. The driver treats a prompt without SEAT as a read seat in the current directory. Do not respond to a refusal by creating directories, changing levels or rerunning with different flags. A measured wrapper created a missing directory and granted unauthorized rights.",
     "words": 56
    },
    "off": {
     "text": "Copy a supplied prompt verbatim, including quotes, dollar signs and header lines; add nothing. The driver treats a prompt without SEAT as a read seat in the current directory. Do not sidestep a refusal by creating directories, changing levels or rerunning with different flags. A measured wrapper created a missing directory and granted unauthorized rights.",
     "words": 55
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "astra-metaphor-6",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/incidents.md:136-140",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "A 20 MB report was truncated at 262144 bytes because stdout had not drained. A Cyrillic answer exceeded a nominal 4000-byte cap, reaching 8003 bytes because .length counts UTF-16 units. A child-exit handler changed a deliberate refusal to exit 4; fail() now marks it settled. Two attempts to obtain prose under --answer-json both returned bare JSON.",
     "words": 56
    },
    "off": {
     "text": "A 20 MB report was truncated at 262144 bytes because stdout had not drained. A Cyrillic answer sailed past a nominal 4000-byte cap, reaching 8003 bytes because .length counts UTF-16 units. A child-exit handler changed a deliberate refusal to exit 4; fail() now marks it settled. Two attempts to obtain prose under --answer-json both returned bare JSON.",
     "words": 57
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   },
   "repeat": true
  },
  {
   "id": "astra-metaphor-7",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/parity.md:36-39",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "A read seat supports native reading, grep, git, node, lint and node-environment vitest when configured with --configLoader runner. Browser-mode vitest fails because Chromium needs an override at the tree root. Composite-project tsc --noEmit also fails when it writes tsbuildinfo. Both failures result from attempted writes outside the permitted TMPDIR.",
     "words": 49
    },
    "off": {
     "text": "A read seat supports native reading, grep, git, node, lint and node-environment vitest when configured with --configLoader runner. Browser-mode vitest hits a wall because Chromium needs an override at the tree root. Composite-project tsc --noEmit also fails when it writes tsbuildinfo. Both failures result from attempted writes outside the permitted TMPDIR.",
     "words": 51
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "astra-metaphor-8",
   "type": "single-factor",
   "source": "research/2026-09-10-chain/chain/README.md:71-73",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "A fresh worktree starts at HEAD, the repository's current commit. Uncommitted edits, untracked files and ignored files are absent, including dependencies installed only in the live checkout. Commit relevant changes before selecting a worktree; stashing alone does not include those changes in HEAD.",
     "words": 43
    },
    "off": {
     "text": "A fresh worktree starts at HEAD, the repository's current commit. Uncommitted edits, untracked files and ignored files are left behind, including dependencies installed only in the live checkout. Commit relevant changes before selecting a worktree; stashing alone does not carry those changes into HEAD.",
     "words": 44
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "astra-answer-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:147-150",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "Selecting a collapsed row removes every member, and the row is removable only if all members are. Items of the same kind with identical displayed names share one numbered row. It shows their count, total size, last-change time span and all paths. On the measured machine, 239 artifacts occupied 98 listing lines.",
     "words": 52
    },
    "off": {
     "text": "Items of the same kind with identical displayed names share one numbered row. It shows their count, total size, last-change time span and all paths. On the measured machine, 239 artifacts occupied 98 listing lines. Selecting a collapsed row removes every member, and the row is removable only if all members are.",
     "words": 52
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "astra-answer-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/SKILL.md:35-40",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "Forward CLAUDE_PLUGIN_DATA under its own name on every driver call. It contains driver state and reported Codex artifacts. CODEX_DELEGATE_STATE_DIR takes precedence; without either variable, the driver exits 2, except for --help. Source installations leave the placeholder empty, so the user's exported CODEX_DELEGATE_STATE_DIR supplies the location.",
     "words": 45
    },
    "off": {
     "text": "It contains driver state and reported Codex artifacts. CODEX_DELEGATE_STATE_DIR takes precedence; without either variable, the driver exits 2, except for --help. Source installations leave the placeholder empty, so the user's exported CODEX_DELEGATE_STATE_DIR supplies the location. Forward CLAUDE_PLUGIN_DATA under its own name on every driver call.",
     "words": 45
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "astra-answer-3",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:142-146",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "A general yes authorizes only suggested removable seat directories for this project and removable test scratch directories. Runs and saved conversations require explicit selection by number. Other projects, unreadable or active items, and the four reported categories cannot be selected. Age appears in the listing but never determines eligibility.",
     "words": 49
    },
    "off": {
     "text": "Runs and saved conversations require explicit selection by number. Other projects, unreadable or active items, and the four reported categories cannot be selected. Age appears in the listing but never determines eligibility. A general yes authorizes only suggested removable seat directories for this project and removable test scratch directories.",
     "words": 49
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "astra-answer-4",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/incidents.md:126-129",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "Treat turn overhead as roughly 7–12 seconds and avoid tuning against an individual measurement. On a busy 36 GB machine, memory exhaustion caused OS termination of delegations. Isolated and host-home overhead measured 6.6 and 8.2 seconds, then 9.2 and 11.7 seconds hours later under less load. Provider round-trips dominate this overhead.",
     "words": 51
    },
    "off": {
     "text": "On a busy 36 GB machine, memory exhaustion caused OS termination of delegations. Isolated and host-home overhead measured 6.6 and 8.2 seconds, then 9.2 and 11.7 seconds hours later under less load. Provider round-trips dominate this overhead. Treat turn overhead as roughly 7–12 seconds and avoid tuning against an individual measurement.",
     "words": 51
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "astra-answer-5",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/incidents.md:211-215",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "Seats no longer inherit a wrapper's wall-clock ceiling; native defaults leave that limit unset. GitHub issue #1, dated September 2, 2026, recorded five of seven seats reaching 540 seconds, with commands using 6–16% of elapsed time and no output returned. The driver added wrap-up steering, interruption grace and partial capture. Silence, command and caller-declared time limits remain explicit.",
     "words": 58
    },
    "off": {
     "text": "GitHub issue #1, dated September 2, 2026, recorded five of seven seats reaching 540 seconds, with commands using 6–16% of elapsed time and no output returned. The driver added wrap-up steering, interruption grace and partial capture. Silence, command and caller-declared time limits remain explicit. Seats no longer inherit a wrapper's wall-clock ceiling; native defaults leave that limit unset.",
     "words": 58
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "astra-answer-6",
   "type": "single-factor",
   "source": "research/2026-09-10-chain/chain/cut-ledger.md:273-278",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "An exitCode of 0 means the turn completed, passed its checks and ran a command. The JSON report ends with the verdict; other codes identify specific complaints documented in --help. Its threadId supports continuation through --resume. The receiptPath and receiptOk fields locate and validate the rollout, with the linked receipt documentation explaining the limits of that evidence.",
     "words": 57
    },
    "off": {
     "text": "The JSON report ends with the verdict; other codes identify specific complaints documented in --help. Its threadId supports continuation through --resume. The receiptPath and receiptOk fields locate and validate the rollout, with the linked receipt documentation explaining the limits of that evidence. An exitCode of 0 means the turn completed, passed its checks and ran a command.",
     "words": 57
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   },
   "repeat": true
  },
  {
   "id": "astra-answer-7",
   "type": "single-factor",
   "source": "research/2026-09-10-chain/chain/audit.md:533-534",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "The repairs were checked structurally and against local implementation, without a new six-reader experiment. No project test suite, model turn, network call, installation, update, cleanup deletion or schema regeneration ran. External plugin commands, skill discovery and login status were not live-tested. Their local provenance was recorded separately from claims about driver implementation.",
     "words": 52
    },
    "off": {
     "text": "No project test suite, model turn, network call, installation, update, cleanup deletion or schema regeneration ran. External plugin commands, skill discovery and login status were not live-tested. Their local provenance was recorded separately from claims about driver implementation. The repairs were checked structurally and against local implementation, without a new six-reader experiment.",
     "words": 52
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "astra-answer-8",
   "type": "single-factor",
   "source": "research/2026-09-10-chain/chain/README.md:75-79",
   "engine": "gpt-6-astra",
   "variants": {
    "on": {
     "text": "Inspect worktree fields even after exit 0; the verdict does not establish harvest success. Completed turns attempt tracked-diff and non-ignored untracked-file exports. Successful harvests are followed by attempted removal; clean trees need no harvest. Ignored files are unarchived and may be lost. Incomplete turns or failed harvests or removals leave the tree preserved.",
     "words": 53
    },
    "off": {
     "text": "Completed turns attempt tracked-diff and non-ignored untracked-file exports. Successful harvests are followed by attempted removal; clean trees need no harvest. Ignored files are unarchived and may be lost. Incomplete turns or failed harvests or removals leave the tree preserved. Inspect worktree fields even after exit 0; the verdict does not establish harvest success.",
     "words": 53
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "astra-cc-1",
   "type": "code-comment",
   "source": "plugins/codex-delegate/skills/seat/scripts/driver.mjs:162-165",
   "engine": "gpt-6-astra",
   "variants": {
    "keep": {
     "text": "  // Settle a refusal before shutdown() SIGTERMs the child, so its exit handler cannot\n  // rewrite the refusal's code to TRANSPORT.",
     "words": 21
    },
    "rewrite": {
     "text": "  // Mark the refusal settled before shutdown; otherwise the child's exit handler can replace its code with TRANSPORT.",
     "words": 18
    },
    "delete": {
     "text": "(no comment)",
     "words": 2
    }
   },
   "code": "  settled = true;\n  throw new Bail();"
  },
  {
   "id": "astra-cc-2",
   "type": "code-comment",
   "source": "plugins/codex-delegate/skills/seat/scripts/driver.mjs:210-213",
   "engine": "gpt-6-astra",
   "variants": {
    "keep": {
     "text": "// The extensions the server takes, and which item kind each becomes.",
     "words": 12
    },
    "rewrite": {
     "text": "// Map supported file extensions to image or audio attachment kinds.",
     "words": 11
    },
    "delete": {
     "text": "(no comment)",
     "words": 2
    }
   },
   "code": "const ATTACH_KINDS = { png: \"localImage\", jpg: \"localImage\", jpeg: \"localImage\", gif: \"localImage\",\n                       webp: \"localImage\", bmp: \"localImage\",\n                       wav: \"localAudio\", mp3: \"localAudio\", m4a: \"localAudio\", ogg: \"localAudio\", flac: \"localAudio\" };"
  },
  {
   "id": "astra-cc-3",
   "type": "code-comment",
   "source": "plugins/codex-delegate/skills/seat/scripts/driver.mjs:549-550",
   "engine": "gpt-6-astra",
   "variants": {
    "keep": {
     "text": "// --help ends on this line and nothing else; the pin in evals/protocol.test.mjs reads it verbatim.",
     "words": 16
    },
    "rewrite": {
     "text": "// Keep this as the final --help line; evals/protocol.test.mjs checks its exact text.",
     "words": 13
    },
    "delete": {
     "text": "(no comment)",
     "words": 2
    }
   },
   "code": "const HELP_POINTER = \"Rarely needed flags, environment variables and internals: --help-all\";"
  },
  {
   "id": "sol-tone-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:162-166",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "Follow the three-command upgrade path: remove the old marketplace, add Nowely/agent-skills as nowely, then install codex-delegate@nowely. Before that, with every seat stopped, move the data directory to the new marketplace name. This orderly sequence preserves existing state; any stale copy is still surfaced by cleanup as another copy's data.",
     "words": 49
    },
    "off": {
     "text": "Follow the three-command upgrade path: remove the old marketplace, add Nowely/agent-skills as nowely, then install codex-delegate@nowely. Before doing so, stop every seat and move the data directory to the new marketplace name. Cleanup lists any stale directory left under the former name as another copy's data.",
     "words": 46
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "sol-tone-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/RELEASING.md:17-26",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "Under the oldest supported Node runtime, run node --check on seat scripts and evaluation files, then npm test. That command includes the package suite, which verifies payload and version agreement. This disciplined sequence catches compatibility and packaging mistakes; require the final count before describing any suite as green.",
     "words": 48
    },
    "off": {
     "text": "Under the oldest supported Node runtime, run node --check on the seat scripts and evaluation files, then run npm test. That command runs every suite, including the package suite, which checks the payload and version agreement. Do not describe a suite as green until its final count has been reported.",
     "words": 50
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "sol-tone-3",
   "type": "single-factor",
   "source": "research/2026-09-10-chain/chain/README.md:86-89",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "By default, a report must include at least one command from the main Codex thread; commands reported only by Codex's subagents do not count. This provenance rule keeps the evidence attributable. Because a failed command or one with unknown outcome can still satisfy the count, careful reviewers inspect its status and exitCode before accepting the run.",
     "words": 56
    },
    "off": {
     "text": "By default, a report must include at least one command item from the main Codex thread. Commands reported only by Codex's subagents do not count toward that requirement. A failed command or a command with an unknown outcome can satisfy the default count, so the reviewer examines its status and exitCode separately.",
     "words": 52
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "sol-tone-4",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:211-213",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "Commands printed by cleanup are safely single-quoted before a user pastes them. That protection matters because a directory named codex-delegate-$(touch PWNED) must remain inert data, not executable syntax. Both the suggested rm command and the find command use shell quoting, since JSON quoting alone does not secure shell input.",
     "words": 49
    },
    "off": {
     "text": "Cleanup single-quotes the rm command it prints for another copy's data directory and the find command for entries beyond its coverage. A directory named codex-delegate-$(touch PWNED) is therefore represented as data when the command is pasted. The implementation uses shell quoting for this purpose; JSON quoting is not shell quoting.",
     "words": 50
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "sol-tone-5",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/parity.md:108-112",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "Each delegation runs its own app server; host-home mode also loads its own MCP servers. Median memory measured about 181 MB per isolated seat and 471 MB with host-home, while provider round-trips dominated 7–12 seconds of overhead. Because excess demand ends runs with SIGTERM, these figures enable safer fan-out: count in-flight delegations, drain waves, and separate writers' working directories.",
     "words": 59
    },
    "off": {
     "text": "Each delegation has its own app server; under host-home, each also loads its own MCP servers. Measured median memory was about 181 MB per isolated seat and 471 MB with host-home, while provider round-trips dominated 7–12 seconds of overhead. Exceeding available memory ends runs with SIGTERM. Count in-flight delegations, drain waves, and separate writers' working directories.",
     "words": 56
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "sol-tone-6",
   "type": "single-factor",
   "source": "plugins/codex-delegate/README.md:220-226",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "Read-level seats run browser tests at write level because Chromium requires a root-level override, while Node-environment Vitest needs --configLoader runner. Concurrency is bounded by memory, and excess runs are killed rather than throttled. Pinning the experimental app-server schema and maintaining a fidelity suite provide a prudent safeguard against an unstable protocol.",
     "words": 51
    },
    "off": {
     "text": "Read-level seats cannot run browser-mode tests because Chromium needs an override written at the tree root; those tests run at write level. Node-environment Vitest at read level needs --configLoader runner. Concurrency is memory-bound, and excess runs are killed rather than throttled. The experimental app-server protocol is covered by a pinned schema and fidelity suite.",
     "words": 54
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "sol-tone-7",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/cleanup/SKILL.md:31-33",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "Each cleanup listing uses mktemp to create its own snapshot file. This reliable isolation matters because a name built from the shell PID can be shared by two listings in one shell, causing a number from the first to be checked against the second. A unique temporary name prevents that cross-listing collision.",
     "words": 52
    },
    "off": {
     "text": "Each cleanup listing uses mktemp to create a separate snapshot file. A filename based on the shell PID can be shared by two listings created in one shell. In that case, a number taken from the first listing could later be read against the second listing's snapshot.",
     "words": 47
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "sol-tone-8",
   "type": "single-factor",
   "source": "research/2026-09-10-chain/chain/README.md:203-208",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "With --seat-file supplying the prompt, a headerless prompt defaults to read level in the current directory; --report-file names an absolute, unused path. The driver creates missing parents at mode 0700 and publishes the complete report at 0600 without replacement. This reliable, no-clobber delivery makes a missing report mean an unknown outcome.",
     "words": 51
    },
    "off": {
     "text": "With --seat-file supplying the prompt, a headerless prompt defaults to read level in the current directory; --report-file names an absolute, unused path. The driver creates missing parents at mode 0700 and publishes the complete report at 0600 without replacing an existing entry. A missing report means the outcome is unknown.",
     "words": 50
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   },
   "repeat": true
  },
  {
   "id": "sol-metaphor-1",
   "type": "single-factor",
   "source": "research/2026-09-10-chain/chain/README.md:167-169",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "For a source installation, retain the state-directory export in the shell profile and launch Claude Code from a shell that inherited it. Source installations do not provide the plugin's data-directory placeholder. Before creating any skill link, inspect its destination and preserve an existing skill rather than overwriting it.",
     "words": 48
    },
    "off": {
     "text": "For a source installation, the shell profile is the bridge that carries the state-directory export into Claude Code. Source installations do not provide the plugin's data-directory placeholder. Before creating any skill link, inspect its destination and leave an existing skill standing rather than overwriting it.",
     "words": 45
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "sol-metaphor-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/RELEASING.md:3-5",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "Publish only from a clean tree. Releases use annotated codex-delegate@X.Y.Z tags with matching GitHub release notes, and a published tag must never be moved or recreated. The repository is a marketplace whose plugins share one tag namespace, so every tag includes the plugin's name.",
     "words": 44
    },
    "off": {
     "text": "A dirty tree is no launchpad for a release. Use annotated codex-delegate@X.Y.Z tags with matching GitHub release notes, and treat every published tag as a mile marker that cannot be moved or recreated. The marketplace's plugins share one tag namespace, so each tag carries the plugin's name.",
     "words": 47
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "sol-metaphor-3",
   "type": "single-factor",
   "source": "research/2026-09-10-chain/chain/README.md:102-106",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "A receipt is Codex's saved session record. The driver checks only opening metadata, within 64 KiB, in today's and yesterday's session directories. receiptOk true establishes that metadata names the reported thread; it does not validate commands, completion, or the answer. False does not change the verdict. Older resumed sessions may fall outside the window.",
     "words": 54
    },
    "off": {
     "text": "A receipt is Codex's saved session record. The driver checks only opening metadata, within 64 KiB, in today's and yesterday's session directories. receiptOk true is a signpost to the reported thread, not a seal on commands, completion, or the answer. False does not change the verdict. Older resumed sessions may fall outside the window.",
     "words": 54
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "sol-metaphor-4",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:255-259",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "A same-user process could still replace the bare leaf between its final file-descriptor check and removal, leaving one instruction interval exposed. Broader replacement cases are prevented: pinned-parent resolution handles renamed ancestors, identity comparison detects a swapped parent, and snapshot device-and-inode data catches a directory replaced between listing and deletion.",
     "words": 49
    },
    "off": {
     "text": "A same-user process could still replace the bare leaf between its final file-descriptor check and removal, leaving one narrow race window open. The design closes the wider windows: pinned-parent resolution blocks renamed ancestors, identity comparison catches a swapped parent, and snapshot device-and-inode data catches a directory replaced between listing and deletion.",
     "words": 51
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "sol-metaphor-5",
   "type": "single-factor",
   "source": "plugins/codex-delegate/RELEASING.md:9-11",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "Choose the release version and set the identical value everywhere the package evaluation compares it: the plugin manifest, the metadata.version line in every skill, and the driver's VERSION constant. If one location retains a different value, the version test fails and identifies the location that was missed.",
     "words": 47
    },
    "off": {
     "text": "Choose the release version, then keep every compared copy marching in lockstep: the plugin manifest, the metadata.version line in every skill, and the driver's VERSION constant. If one copy falls out of step, the version test fails and points directly to the location that was missed.",
     "words": 46
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "sol-metaphor-6",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/parity.md:41-43",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "A worktree contains repository HEAD rather than uncommitted files in the current tree, so relevant work must first be committed or stashed; alternatively, use write level in the repository after settling its scope. Dependencies and ignored files are absent, and a verifier that requires them exits with status 1 unless they are installed in the worktree.",
     "words": 56
    },
    "off": {
     "text": "A worktree is a time capsule of repository HEAD, leaving current uncommitted work behind. Relevant changes must first be committed or stashed; alternatively, use write level in the repository after settling its scope. Dependencies and ignored files travel separately, so a verifier that needs them exits with status 1 unless they are installed in the worktree.",
     "words": 56
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "sol-metaphor-7",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/parity.md:123-125",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "Do not background a wrapper that forks driver calls and then exits. The harness tracks the wrapper process; after it exits, its children are reparented and their results do not return. A non-login shell needs no manual PATH export, because the driver locates codex through PATH and standard installation directories.",
     "words": 50
    },
    "off": {
     "text": "Do not background a wrapper that forks driver calls and then exits. The harness tracks the wrapper process; after it exits, its children are reparented and their results fall through the cracks. A non-login shell needs no manual PATH export, because the driver locates codex through PATH and standard installation directories.",
     "words": 51
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "sol-metaphor-8",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/cleanup/SKILL.md:61-66",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "Do not rerun cleanup's listing between approval and deletion. The saved snapshot associates each selected number with the item the user saw, and anything changed afterward is retained and reported. The deletion command then prints one paragraph for every outcome followed by a fresh listing; report those outcomes using its names and reasons, without additions.",
     "words": 55
    },
    "off": {
     "text": "Do not rerun cleanup's listing between approval and deletion. The saved snapshot anchors each selected number to the item the user saw, and anything changed afterward is retained and reported. The deletion command then prints one paragraph for every outcome followed by a fresh listing; report those outcomes using its names and reasons, without additions.",
     "words": 55
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   },
   "repeat": true
  },
  {
   "id": "sol-answer-1",
   "type": "single-factor",
   "source": "research/2026-09-10-chain/chain/README.md:91-93",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "Use allow-no-commands only when commands are unnecessary; it does not waive a declared expect-command check. Exit 0 can otherwise be produced without any reported command. The command-line flag or the matching seat-file header waives the default command count, as when recalling an earlier answer. When is that waiver appropriate?",
     "words": 49
    },
    "off": {
     "text": "Exit 0 can otherwise be produced without any reported command. The command-line flag or the matching seat-file header waives the default command count, as when recalling an earlier answer. When is that waiver appropriate? Use allow-no-commands only when commands are unnecessary; it does not waive a declared expect-command check.",
     "words": 49
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "sol-answer-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:249-251",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "Match the selected row in the fresh inventory by kind, name, and complete snapshot contents, never by its position. A number grants consent only for the item it named. Because cleanup reruns the inventory for each number, removing one row renumbers everything after it. How can later selections retain their original meaning?",
     "words": 52
    },
    "off": {
     "text": "A number grants consent only for the item it named. Because cleanup reruns the inventory for each number, removing one row renumbers everything after it. How can later selections retain their original meaning? Match the selected row in the fresh inventory by kind, name, and complete snapshot contents, never by its position.",
     "words": 52
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "sol-answer-3",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/SKILL.md:170-173",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "Give each seat one deliverable. A requested return may combine unrelated artifacts or decisions. Its first line must stand alone, naming the agent's model and id, status, and completed work; the coordinator retells that line, which is not itself addressed to the user. How should such a return be scoped?",
     "words": 50
    },
    "off": {
     "text": "A requested return may combine unrelated artifacts or decisions. Its first line must stand alone, naming the agent's model and id, status, and completed work; the coordinator retells that line, which is not itself addressed to the user. How should such a return be scoped? Give each seat one deliverable.",
     "words": 50
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "sol-answer-4",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/SKILL.md:175-179",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "Do not repeat the standing rules in a seat prompt. The thread already specifies unattended operation, egress and search choices, COMMAND_BLOCKED behavior, and the requirement for a test count. A follow-up uses RESUME with the thread id, while recall-only work runs no commands and needs ALLOW_NO_COMMANDS. What belongs in the new prompt?",
     "words": 52
    },
    "off": {
     "text": "The thread already specifies unattended operation, egress and search choices, COMMAND_BLOCKED behavior, and the requirement for a test count. A follow-up uses RESUME with the thread id, while recall-only work runs no commands and needs ALLOW_NO_COMMANDS. What belongs in the new prompt? Do not repeat the standing rules in a seat prompt.",
     "words": 52
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "sol-answer-5",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/cleanup/SKILL.md:48-52",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "A plain affirmative selects exactly the numbers in proposed. Digits select those numbers, an affirmative with digits adds them to the proposal, and only or instead restricts selection to the digits. The word all is ambiguous because runs and saved conversations may also be selectable. How should the user's approval be mapped?",
     "words": 52
    },
    "off": {
     "text": "Digits select those numbers, an affirmative with digits adds them to the proposal, and only or instead restricts selection to the digits. The word all is ambiguous because runs and saved conversations may also be selectable. How should the user's approval be mapped? A plain affirmative selects exactly the numbers in proposed.",
     "words": 52
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "sol-answer-6",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/parity.md:60-63",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "Network access and file-write rights are independent. Seats at both levels can reach the network unless NETWORK is no, and no host allowlist narrows that grant. A read seat still writes only to TMPDIR, while anything it can read can be sent. What changes when network access is disabled?",
     "words": 49
    },
    "off": {
     "text": "Seats at both levels can reach the network unless NETWORK is no, and no host allowlist narrows that grant. A read seat still writes only to TMPDIR, while anything it can read can be sent. What changes when network access is disabled? Network access and file-write rights are independent.",
     "words": 49
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "sol-answer-7",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/parity.md:129-132",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "Run Chromium with --single-process under the seatbelt sandbox. The default profile denies mach-register, so bootstrap_check_in fails in the browser process. In one measured repository, the override allowed the full suite to complete with 2,518 passing tests across 121 files, the same count as the unsandboxed reference. What configuration avoids the failure?",
     "words": 51
    },
    "off": {
     "text": "The default profile denies mach-register, so bootstrap_check_in fails in the browser process. In one measured repository, the override allowed the full suite to complete with 2,518 passing tests across 121 files, the same count as the unsandboxed reference. What configuration avoids the failure? Run Chromium with --single-process under the seatbelt sandbox.",
     "words": 51
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "sol-answer-8",
   "type": "single-factor",
   "source": "research/2026-09-10-chain/chain/README.md:120-125",
   "engine": "gpt-5.6-sol",
   "variants": {
    "on": {
     "text": "A successful check establishes structural compliance, not factual correctness. The top level must be an object, every object's properties must be required, and additionalProperties must be false. The report identifies keywords the driver's validator did not recheck. What does a successfully constrained answer prove?",
     "words": 44
    },
    "off": {
     "text": "The top level must be an object, every object's properties must be required, and additionalProperties must be false. The report identifies keywords the driver's validator did not recheck. What does a successfully constrained answer prove? A successful check establishes structural compliance, not factual correctness.",
     "words": 44
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   },
   "repeat": true
  },
  {
   "id": "sol-cc-1",
   "type": "code-comment",
   "source": "plugins/codex-delegate/skills/seat/scripts/driver.mjs:708-711",
   "engine": "gpt-5.6-sol",
   "variants": {
    "keep": {
     "text": "// Asking for help is not a usage error: it goes to stdout and exits 0, so `--help | head` works.\n// No process.exit() behind the write: on an asynchronous pipe (macOS) that truncates the text.",
     "words": 36
    },
    "rewrite": {
     "text": "// Help exits successfully and writes asynchronously so piped consumers such as `head` receive the complete output.",
     "words": 17
    },
    "delete": {
     "text": "(no comment)",
     "words": 2
    }
   },
   "code": "      case \"-h\": case \"--help\": case \"--help-all\":\n        process.stdout.write(helpText(a === \"--help-all\")); process.exitCode = EXIT.OK; settled = true; throw new Bail();"
  },
  {
   "id": "sol-cc-2",
   "type": "code-comment",
   "source": "plugins/codex-delegate/skills/seat/scripts/driver.mjs:725-727",
   "engine": "gpt-5.6-sol",
   "variants": {
    "keep": {
     "text": "// 0 is the documented \"off\", like --idle-timeout's.",
     "words": 8
    },
    "rewrite": {
     "text": "// Accept zero to disable the command limit, matching the idle-timeout convention.",
     "words": 12
    },
    "delete": {
     "text": "(no comment)",
     "words": 2
    }
   },
   "code": "  if (!Number.isInteger(o.maxCommands) || o.maxCommands < 0)\n    fail(EXIT.USAGE, \"--max-commands must be a whole number of commands, 0 to disable\");"
  },
  {
   "id": "sol-cc-3",
   "type": "code-comment",
   "source": "plugins/codex-delegate/skills/seat/scripts/driver.mjs:1118-1122",
   "engine": "gpt-5.6-sol",
   "variants": {
    "keep": {
     "text": "// Buffer.from salvages malformed base64, so re-encode to detect corruption and fail closed\n// instead of treating binary noise as an unconstrained policy.",
     "words": 23
    },
    "rewrite": {
     "text": "// Re-encode the decoded bytes to reject malformed base64 instead of interpreting corruption as an unrestricted policy.",
     "words": 17
    },
    "delete": {
     "text": "(no comment)",
     "words": 2
    }
   },
   "code": "  const raw = r.stdout.trim();\n  const buf = Buffer.from(raw, \"base64\");\n  if (buf.toString(\"base64\").replace(/=+$/, \"\") !== raw.replace(/=+$/, \"\")) return undefined;"
  },
  {
   "id": "terraA-tone-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:333-336",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "`--resume last` selects the most recently started run for the current `--cwd`, or for the repository when `--worktree` is used. This avoids letting a long-running seat overshadow a shorter, later run that has already finished, while preserving exit 10 when the newest run is still active.",
     "words": 46
    },
    "off": {
     "text": "`--resume last` selects the most recently started run for the current `--cwd`, or for the repository with `--worktree`. It does not select the most recently written run. A long-running seat therefore cannot outrank a shorter, later run that has finished; an active newest run still produces exit 10.",
     "words": 48
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "terraA-tone-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/evals/README.md:14-19",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "The runner lists twelve suites in cost order and refuses to start if that list differs from the directory, so unlisted suites cannot be missed. It treats a signal-killed suite as a failure and removes skipped or unrun suites from the green count, naming them in the final line.",
     "words": 49
    },
    "off": {
     "text": "The runner lists twelve suites in cost order and refuses to start if its list differs from the directory. A signal-killed suite is a failure. Skipped or unrun suites do not count as green, and the final line names them. This prevents a suite absent from the list from going unrun.",
     "words": 51
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "terraA-tone-3",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/environment-and-internals.md:23-26",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "The report’s `tokenUsage` covers the root thread and excludes Codex subagent threads. With `--resume`, `total` is cumulative for the thread, while `last` is only the most recent API request. This makes `total` useful as a turn-cost measure rather than confusing its final request with the whole turn.",
     "words": 47
    },
    "off": {
     "text": "The report’s `tokenUsage` covers the root thread and excludes Codex subagent threads. With `--resume`, `total` is cumulative for the thread, while `last` is only the most recent API request. `total` measures the thread’s cost across a turn, whereas `last` describes only the final request.",
     "words": 44
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "terraA-tone-4",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/cleanup/SKILL.md:91-98",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "Cleanup lists managed worktrees and their ledger, write locks, and the shared Codex home but never removes them. The driver reconciles the first two, and every seat shares the home. For another plugin copy’s data directory, it shows the manual command, keeping ownership and the applicable removal boundary clear.",
     "words": 49
    },
    "off": {
     "text": "Cleanup lists but never removes managed worktrees and their ledger, write locks, or the shared Codex home. The driver reconciles the worktrees and locks, and every seat shares the home. Another plugin copy’s data directory remains the user’s to remove; its manual command is shown when requested.",
     "words": 47
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "terraA-metaphor-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:385-388",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "Orchestrate stores each run directory in the plugin data directory, at `${CLAUDE_PLUGIN_DATA}/orchestrate/<project-slug>/<run>/`. The project slug is the working directory path as Claude Code spells it under `~/.claude/projects/`. As a result, a run leaves its working tree unchanged and no longer needs the old self-ignoring `.gitignore`.",
     "words": 45
    },
    "off": {
     "text": "Orchestrate moves each run directory out of the repository and into `${CLAUDE_PLUGIN_DATA}/orchestrate/<project-slug>/<run>/`. The project slug follows Claude Code’s spelling of the working directory under `~/.claude/projects/`. The move keeps a run’s footprints out of its working tree, so the old self-ignoring `.gitignore` can disappear.",
     "words": 43
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "terraA-metaphor-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/why-not-the-plugin.md:46-49",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "On unmanaged machines, the official plugin’s `--write` path can handle in-repository tasks because `workspace-write` opens `$TMPDIR` and `/tmp`. Read and review seats, however, cannot run tests on any machine. Their failure is silent at the crucial point: a review can return a confident verdict after running nothing.",
     "words": 47
    },
    "off": {
     "text": "On unmanaged machines, the official plugin’s `--write` path is usable for in-repository tasks because `workspace-write` opens `$TMPDIR` and `/tmp`. Read and review seats hit a wall everywhere: they cannot run tests. Worse, the failure stays hidden where it matters, returning a confident review that has run nothing.",
     "words": 47
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "terraA-metaphor-3",
   "type": "single-factor",
   "source": "research/2026-09-10-chain/chain/audit.md:23-25",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "The third pass identified 24 hidden prerequisites. It defined terms and path placeholders where needed, moved required state before the first task, and separated optional configuration, prompt instructions, automated gates, source exports, and the repository root. The pass added 105 words to supply missing framing rather than implementation background.",
     "words": 49
    },
    "off": {
     "text": "The third pass unearthed 24 hidden prerequisites. It defined terms and path placeholders where needed, moved required state before the first task, and separated optional configuration, prompt instructions, automated gates, source exports, and the repository root. It added 105 words to fill missing framing instead of piling on implementation background.",
     "words": 50
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "terraA-metaphor-4",
   "type": "single-factor",
   "source": "research/2026-09-10-chain/chain/README.md:262-270",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "At read level, the driver requests `on-request` approval and omits the explicit sandbox parameter so the temporary-directory grant can apply. It verifies the reported permissions before continuing. The comparison with the official plugin records tests dated 2026-08-31 against version 1.0.6 and Codex CLI 0.150.1, and advises checking current behavior.",
     "words": 49
    },
    "off": {
     "text": "At read level, the driver requests `on-request` approval and leaves out the explicit sandbox parameter, letting the temporary-directory grant come through. It checks reported permissions before proceeding. Its comparison with the official plugin, frozen on 2026-08-31 against version 1.0.6 and Codex CLI 0.150.1, should not be treated as today’s behavior.",
     "words": 50
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "terraA-answer-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:360-363",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "The app-server now starts with `TMPPREFIX` inside the run’s `TMPDIR`, allowing zsh here-documents to create temporary files. Previously, every seat here-document failed because zsh used its default `/tmp/zsh`, which no grant covered. The failure was measured in 15 rollouts between 2026-08-31 and 2026-09-08 and confirmed by a live seat.",
     "words": 49
    },
    "off": {
     "text": "Previously, every seat here-document failed because zsh used its default `/tmp/zsh`, which no grant covered. The failure was measured in 15 rollouts between 2026-08-31 and 2026-09-08 and confirmed by a live seat. The app-server now starts with `TMPPREFIX` inside the run’s `TMPDIR`, allowing zsh here-documents to create temporary files.",
     "words": 49
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "terraA-answer-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/RELEASING.md:46-50",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "The delegation probe checks every announced `subagentThreads` child for an `agentPath`, completed status, and at least one command; an exit 5 cause names those children. After a Codex upgrade, its environment variable spends a second Codex turn on a probe that invites the seat to delegate. Delegation remains the model’s choice and is not effort-gated.",
     "words": 55
    },
    "off": {
     "text": "After a Codex upgrade, its environment variable spends a second Codex turn on a probe that invites the seat to delegate. Delegation remains the model’s choice and is not effort-gated. The delegation probe checks every announced `subagentThreads` child for an `agentPath`, completed status, and at least one command; an exit 5 cause names those children.",
     "words": 55
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "terraA-answer-3",
   "type": "single-factor",
   "source": "plugins/codex-delegate/evals/README.md:29-34",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "A case that cannot run returns the harness’s `skip(reason)` sentinel, which prints its reason, counts as neither pass nor fail, and is named in the summary. The documentation omits suite counts because the last count was wrong twice in two days; each `CASES` array is the inventory, and each suite’s final line states its count.",
     "words": 55
    },
    "off": {
     "text": "The documentation omits suite counts because the last count was wrong twice in two days; each `CASES` array is the inventory, and each suite’s final line states its count. A case that cannot run returns the harness’s `skip(reason)` sentinel, which prints its reason, counts as neither pass nor fail, and is named in the summary.",
     "words": 55
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "terraA-answer-4",
   "type": "single-factor",
   "source": "research/2026-09-10-chain/chain/README.md:246-254",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "After upgrading Codex, run `node evals/fidelity.test.mjs --require-live`. The test compares the real Codex connection handshake with the test server. With `--require-live`, a missing Codex binary is a failure; without it, missing-binary cases skip. This option does not request a model turn, and paid checks require separate opt-ins.",
     "words": 47
    },
    "off": {
     "text": "The test compares the real Codex connection handshake with the test server. With `--require-live`, a missing Codex binary is a failure; without it, missing-binary cases skip. This option does not request a model turn, and paid checks require separate opt-ins. After upgrading Codex, run `node evals/fidelity.test.mjs --require-live`.",
     "words": 47
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "terraA-cc-1",
   "type": "code-comment",
   "source": "plugins/codex-delegate/skills/seat/scripts/driver.mjs:1401-1403",
   "engine": "gpt-5.6-terra",
   "variants": {
    "keep": {
     "text": "// Hash the directory's dev:ino identity so case variants and symlinks share a lock.\n// Accept the caller's stat to avoid another lookup; export the key so tests use the same rule.",
     "words": 32
    },
    "rewrite": {
     "text": "// Derive one lock name from the directory identity, shared by symlink and case aliases.",
     "words": 15
    },
    "delete": {
     "text": "(no comment)",
     "words": 2
    }
   },
   "code": "const lockKey = (st) => `${crypto.createHash(\"sha256\").update(`${st.dev}:${st.ino}`).digest(\"hex\")}.lock`;"
  },
  {
   "id": "terraA-cc-2",
   "type": "code-comment",
   "source": "plugins/codex-delegate/skills/seat/scripts/driver.mjs:1435-1437",
   "engine": "gpt-5.6-terra",
   "variants": {
    "keep": {
     "text": "// A lock that vanished between create and read was released by a peer; retry acquisition.",
     "words": 16
    },
    "rewrite": {
     "text": "// Retry when another process releases the lock before inspection.",
     "words": 10
    },
    "delete": {
     "text": "(no comment)",
     "words": 2
    }
   },
   "code": "const seen = inspectLock(p, dir);\nif (seen.gone) { sleepSync(LIMITS.LOCK_RETRY_MS); continue; }"
  },
  {
   "id": "terraB-tone-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/environment-and-internals.md:69-72",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "Unless `--host-home` is used, each run uses the private `CODEX_HOME` at `<state>/home`. Sharing it across runs, rather than recreating it for each turn, retains Codex caches and databases and makes isolated runs faster. The caller’s plugins, skills, and MCP servers stay out, and no trust records are written back.",
     "words": 49
    },
    "off": {
     "text": "Unless `--host-home` is used, each run uses the private `CODEX_HOME` at `<state>/home`. It is shared across runs on the machine rather than recreated for each turn. Codex caches and databases live there, while the caller’s plugins, skills, and MCP servers stay out and no trust records are written back.",
     "words": 49
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "terraB-tone-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:433-436",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "On macOS with Node 24.11, the project was measured against codex-cli 0.153.4. The pinned protocol moves from 0.150.1 to 0.153.4. Its additive protocol diff adds nine type files, changes thirty, and removes none, easing assessment. Orchestrate and its evals were measured on 0.153.4 before the pin moved.",
     "words": 47
    },
    "off": {
     "text": "On macOS with Node 24.11, the project was measured against codex-cli 0.153.4. The pinned protocol moves from 0.150.1 to 0.153.4. Its purely additive protocol diff adds nine type files, changes thirty, and removes none. Orchestrate and its evals were measured on 0.153.4 before the pin moved.",
     "words": 46
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "terraB-tone-3",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/why-not-the-plugin.md:92-97",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "Both the driver and plugin launch `codex app-server` and exchange thread, turn, and item JSON-RPC over stdio. The driver provides a dependable rights layer: it gives schema-correct approval refusals, pins the reviewer, checks applied policy, and derives its exit code from events. Plugin returns `-32601` for server-to-client requests and asserts nothing about rights; upstream has not merged this layer.",
     "words": 59
    },
    "off": {
     "text": "Both the driver and plugin launch `codex app-server` and exchange thread, turn, and item JSON-RPC over stdio. The plugin returns `-32601` for server-to-client requests and asserts nothing about rights. The driver gives schema-correct approval refusals, pins the reviewer, checks applied policy, and derives its exit code from events; upstream has not merged this layer.",
     "words": 54
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "terraB-tone-4",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:414-417",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "The eleven suites run from cheapest to most expensive, for faster feedback. Protocol separates server-event behaviour into 135 cases from CLI arguments and output into 85. Lock has 53 cases and worktree 22, using the new scenarios library. Each protocol-table case gets its own state root for isolation.",
     "words": 48
    },
    "off": {
     "text": "The eleven suites run from cheapest to most expensive. Protocol separates server-event behaviour into 135 cases from CLI arguments and output into 85. Lock has 53 cases and worktree has 22, using the new scenarios library. Every protocol-table case uses its own state root.",
     "words": 44
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "terraB-metaphor-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/evals/README.md:59-62",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "A delegation skill can fail without an error in two ways. If it does not activate, work that needs a second independent opinion receives only one Claude opinion. If it activates for every question, each trivial question incurs a seat’s turn overhead and memory cost. Neither condition reports an error.",
     "words": 50
    },
    "off": {
     "text": "A delegation skill can fail silently in two directions. If it never wakes up, work that needs a second independent opinion gets only one Claude’s view. If it wakes for every question, each trivial question carries a whole seat’s turn overhead and memory cost. Neither failure raises an alarm.",
     "words": 49
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "terraB-metaphor-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/environment-and-internals.md:102-105",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "The format does not construct shell commands from relayed values: an injected quote remains literal text rather than becoming a flag. Attachments, bounds, and `--report-file` are command-line-only because injected fields could otherwise upload, truncate, or redirect a run the user did not name. A header naming one exits 2 and identifies the needed flag.",
     "words": 54
    },
    "off": {
     "text": "The format keeps relayed values out of shell-command construction: an injected quote is defanged rather than turned into a flag. Attachments, bounds, and `--report-file` are command-line-only because injected fields could otherwise upload, truncate, or redirect a run the user did not name. A header naming one exits 2 and identifies the needed flag.",
     "words": 53
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "terraB-metaphor-3",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:470-473",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "The driver registers a Codex subagent thread from the root’s `subAgentActivity` announcement. In 0.153.4, children do not send `thread/started`, so the earlier registration did not run. A delegating turn reported no subagent threads or child work, and the idle guard could not observe work while children ran.",
     "words": 47
    },
    "off": {
     "text": "The driver picks up a Codex subagent thread from the root’s `subAgentActivity` announcement. In 0.153.4, children never send `thread/started`, so the earlier registration never caught them. A delegating turn reported no subagent threads or child work, and the idle guard was blind while children worked.",
     "words": 45
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "terraB-metaphor-4",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/why-not-the-plugin.md:115-122",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "The plugin provides a persisted background-job index with logs, session-scoped status and cancel, a Stop-hook review gate, native `review/start`, shared-broker `turn/interrupt`, and Claude-session import. This driver uses only `turn/interrupt` for signals. Seats last only for caller tasks, and the driver validates review output instead of trusting server output.",
     "words": 48
    },
    "off": {
     "text": "The plugin brings a persisted background-job index with logs, session-scoped status and cancel, a Stop-hook review gate, native `review/start`, shared-broker `turn/interrupt`, and Claude-session import. This driver takes only `turn/interrupt` for signals. It keeps seats tied to caller tasks and validates review output itself instead of trusting server output.",
     "words": 48
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "terraB-answer-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:458-460",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "Sending `excludeTurns: true` makes resume requests much smaller in measurement: size fell from 1.5 MB to 58 KB. The driver never reads `thread.turns`; every thread from 0.153.4 is paginated, and an ephemeral fork without the flag was refused with -32600.",
     "words": 40
    },
    "off": {
     "text": "The driver never reads `thread.turns`; every thread from 0.153.4 is paginated, and an ephemeral fork without `excludeTurns: true` was refused with -32600. Therefore, sending `excludeTurns: true` makes resume requests much smaller in measurement: size fell from 1.5 MB to 58 KB.",
     "words": 41
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "terraB-answer-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/evals/README.md:89-94",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "Count only the complete `\"skill\":\"…\"` value; it is the verdict. The bare word `seat` appears in skill listings and repository prose, so it can match even where the skill never fired. Request a self-report as a cross-check, but do not treat it as evidence of tools used.",
     "words": 47
    },
    "off": {
     "text": "The bare word `seat` appears in skill listings and repository prose, so it can match even where the skill never fired. A self-report is only a cross-check, not evidence of tools used. Therefore, count only the complete `\"skill\":\"…\"` value; it is the verdict.",
     "words": 43
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "terraB-answer-3",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/environment-and-internals.md:74-81",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "A seat that needs caller MCP servers must use `--host-home`, accepting host configuration. `auth.json` and sessions are symlinked to `~/.codex`; `model`, `model_reasoning_effort`, `personality`, and `service_tier` arrive through `config/read`. A failed probe warns, retries, retains its last good config, and a terminating run writes nothing. `configInherited` reports its source and carried keys; reports include actual and pinned versions.",
     "words": 57
    },
    "off": {
     "text": "`auth.json` and sessions are symlinked to `~/.codex`; `model`, `model_reasoning_effort`, `personality`, and `service_tier` arrive through `config/read`. A failed probe warns, retries, retains its last good config, and a terminating run writes nothing. `configInherited` reports its source and carried keys; reports include actual and pinned versions. Therefore, a seat needing caller MCP servers must use `--host-home`, accepting host configuration.",
     "words": 57
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "terraB-answer-4",
   "type": "single-factor",
   "source": "research/2026-09-10-chain/chain/invisible-prerequisites.md:23-27",
   "engine": "gpt-5.6-terra",
   "variants": {
    "on": {
     "text": "`TASK`, `CHECK`, and `RETURN` are model instructions, not executable verification. Driver configuration instead comes from CLI flags and leading seat-file headers. A thread is a Codex conversation, while the command floor applies only to its main thread. Exit 0 summarizes implemented checks; a command item can have failed or lack status.",
     "words": 51
    },
    "off": {
     "text": "CLI flags and leading seat-file headers configure the driver. A thread is a Codex conversation, while the command floor applies only to its main thread. Exit 0 summarizes implemented checks; a command item can have failed or lack status. Therefore, `TASK`, `CHECK`, and `RETURN` are model instructions, not executable verification.",
     "words": 50
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "terraB-cc-1",
   "type": "code-comment",
   "source": "plugins/codex-delegate/skills/seat/scripts/driver.mjs:1684-1686",
   "engine": "gpt-5.6-terra",
   "variants": {
    "keep": {
     "text": "// Guard the repository before git worktree add can mutate a protected root.",
     "words": 13
    },
    "rewrite": {
     "text": "// Validate the repository root before creating a worktree.",
     "words": 9
    },
    "delete": {
     "text": "(no comment)",
     "words": 2
    }
   },
   "code": "function createWorktree(repo, prior = null) {\n  checkRoot(repo);\n}"
  },
  {
   "id": "opusB-tone-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:575-577",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "Two flags are new: `--relay <file>` and `--relay-collect <threadId>`. The driver launches a single detached seat and renders one text envelope under the run's own exit code, so a caller reads one shape. A running envelope carries the collection command, ready to repeat without reconstructing it.",
     "words": 46
    },
    "off": {
     "text": "Two flags are new: `--relay <file>` and `--relay-collect <threadId>`. The driver launches a single detached seat, waits for it, and renders one text envelope under the run's own exit code. While the seat is still running, that envelope carries the complete collection command to repeat.",
     "words": 45
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "opusB-tone-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:606-609",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "Exit 11 now also covers a command that reached the client with no verdict, so silence is no longer scored as success. The probe exemption is judged on the command the server parsed, which spares a `grep` that matches nothing. Verifier output is streamed, so a loud verifier that exits `0` passes.",
     "words": 52
    },
    "off": {
     "text": "Exit 11 now also covers a command that reached the client with no verdict. The probe exemption is judged on the command the server parsed, so a `grep` that matches nothing no longer raises it. Verifier output is streamed rather than buffered, and a loud verifier that exits `0` passes.",
     "words": 50
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   },
   "repeat": true
  },
  {
   "id": "opusB-tone-3",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/environment-and-internals.md:244-250",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "The driver's own git is not exposed to what a seat writes in its tree. Every git it spawns disables fsmonitor, hooks and external diff; each call is bounded and ends in `SIGKILL`. That keeps a seat's code from running with the caller's rights, as harvest and checkout once let it.",
     "words": 51
    },
    "off": {
     "text": "The driver's own git is not exposed to what a seat writes in its tree. Every git it spawns disables fsmonitor, hooks and external diff; each call is bounded and ends in `SIGKILL`. Before that, harvest and the next checkout ran the seat's hooks with the caller's rights.",
     "words": 48
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "opusB-tone-4",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/adversarial-review.md:25-28",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "A finding names what can go wrong, why the path is vulnerable, the likely impact, the file and line range, a confidence, and a concrete change, so the caller can act on it directly. One strong finding is worth more than several weak ones; invented incidents or results waste the review.",
     "words": 51
    },
    "off": {
     "text": "A finding names what can go wrong, why the path is vulnerable, the likely impact, the affected file and line range, a confidence from 0 to 1, and a concrete risk-reducing change. Prefer one strong finding to several weak ones, and never invent files, incidents, runtime behaviour, or test results.",
     "words": 50
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "opusB-metaphor-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/evals/README.md:225-229",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "`fake-app-server.mjs` is still the only reference behind every protocol and lock assertion, and three fixture failures have already let the suite pass while the code was wrong: schemas were not strict, commands were emitted bare while the live server wraps each one in a shell, and a review payload was an object rather than the live string.",
     "words": 57
    },
    "off": {
     "text": "`fake-app-server.mjs` is still the oracle behind every protocol and lock assertion, and three fixture failures have already kept false confidence green: schemas were not strict, commands were emitted bare while the live server wraps each one in a shell, and a review payload was invented as an object rather than the live string.",
     "words": 53
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "opusB-metaphor-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/environment-and-internals.md:210-214",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "The lock is released only after the driver has waited its child process group out: SIGTERM, a short bounded wait, then SIGKILL and a shorter one. The next writer therefore does not start in a directory where the previous run's test servers have not yet exited. A `--worktree` turn ends that group even earlier.",
     "words": 54
    },
    "off": {
     "text": "The lock is released only after the driver has waited its child process group out: SIGTERM, a short bounded wait, then SIGKILL and a shorter one. The next writer therefore does not walk into a directory where the previous run's test servers are still dying. A `--worktree` turn quiesces that group even earlier.",
     "words": 53
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   },
   "repeat": true
  },
  {
   "id": "opusB-metaphor-3",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/environment-and-internals.md:254-256",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "Misspelled configuration keys are accepted without any error by both `-c` and the app-server. `tools.web_search` is a real key whose name resembles the web-search switch and does nothing; the effective one is top-level `web_search`, which the driver sets to whatever `--web-search` asked for, and to `disabled` only when the flag is absent.",
     "words": 52
    },
    "off": {
     "text": "Misspelled configuration keys are swallowed silently by both `-c` and the app-server. `tools.web_search` is a real key that wears the web-search switch's face and does nothing; the live one is top-level `web_search`, which the driver sets to whatever `--web-search` asked for, and to `disabled` only when the flag is absent.",
     "words": 50
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "opusB-metaphor-4",
   "type": "single-factor",
   "source": "plugins/codex-delegate/evals/README.md:240-245",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "Covered since: resume, where the protocol suite pins resume-busy and a resumed seat's budget; the stdout drain path, with a closed pipe and a paused one during a large report; `--host-home`, where a TERM-ignoring descendant is killed and the lock released; the exit ladder, one case per exit code; and Linux, tested by CI on every push.",
     "words": 57
    },
    "off": {
     "text": "Struck since: resume, where the protocol suite pins resume-busy and a resumed seat's budget; the stdout drain path, with a closed pipe and a paused one during a large report; `--host-home`, where a TERM-ignoring descendant is swept and the lock released; the exit ladder, one case per rung; and Linux, now a matrix leg on every push.",
     "words": 57
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "opusB-answer-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/result-gates.md:81-83",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "A verifier that exits `0` while a background process still holds its stdout counts as a pass. The deadline fires on the pipe, not on the command, and the exit status was observed, which is the proof. Keep verifiers cheap and quiet anyway: `test -f`, `grep -q`, one targeted test project.",
     "words": 51
    },
    "off": {
     "text": "A verifier runs under a deadline, and the driver reads its exit status. Sometimes a background process keeps the verifier's stdout open, so the deadline fires on the pipe after the command finished. Is that a failure? No: the observed `0` is proof and counts as a pass. Keep verifiers cheap anyway.",
     "words": 52
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "opusB-answer-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/evals/README.md:205-212",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "Cases 20 and 22 are inconclusive, not failed; score them only with a real diff in a real repository and full tools. Under the 2026-08-31 cheap harness, a prompt about «этот дифф» with no diff attached spent both turns on a denied `git diff`. Case 21 fired, case 23 stayed silent.",
     "words": 51
    },
    "off": {
     "text": "The 2026-08-31 run gave each case two turns and `Skill` alone. Case 21 fired, case 23 stayed silent. But a prompt about «этот дифф» carried no diff, so cases 20 and 22 spent both turns on a denied `git diff`. Failures? No: inconclusive, rescorable only with a real diff and full tools.",
     "words": 52
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   },
   "repeat": true
  },
  {
   "id": "opusB-answer-3",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/environment-and-internals.md:222-226",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "A seat cannot commit under the grant a `SEAT:` line makes: no git directory is granted by default, and committing needs the main clone's common dir. Measured in a linked worktree whose main `.git` was read-only, `git commit` failed on a denied `index.lock`. Work returns as a diff and an archive.",
     "words": 51
    },
    "off": {
     "text": "A `SEAT:` line grants a writable root, and a seat asked to finish a change runs `git commit`. Committing needs the main clone's common dir, which no default grant includes. Does it land? No: measured in a worktree with a read-only `.git`, it failed on a denied `index.lock`. Work returns as a diff and an archive.",
     "words": 56
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "opusB-answer-4",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:589-591",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "The relay stays pinned to sonnet. Re-measured on the final body, sonnet passed the header-less, refused-write and repeated-collection cases three times out of three. Haiku did relay envelopes and collection commands correctly, but it still added header fields to prompts that had none, which is the one thing the relay must not do.",
     "words": 53
    },
    "off": {
     "text": "The final relay body was re-measured on both candidate models, across the header-less, refused-write and repeated-collection cases. Sonnet passed three of three. Haiku relayed envelopes and collection commands correctly, yet still added header fields to prompts that had none. Which model carries the relay? Sonnet, and the pin stays.",
     "words": 49
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "opusB-cc-1",
   "type": "code-comment",
   "source": "plugins/codex-delegate/skills/seat/scripts/driver.mjs:2325-2328",
   "engine": "claude-opus-5",
   "variants": {
    "keep": {
     "text": "// kills the group once and settles the probe",
     "words": 9
    },
    "rewrite": {
     "text": "// the probe has its answer; this kills its group and rejects anything still pending",
     "words": 15
    },
    "delete": {
     "text": "(no comment)",
     "words": 2
    }
   },
   "code": "function shutdown() {\n  if (shutdownDone) return shutdownDone;\n  shutdownDone = (async () => {\n    if (probeConn) probeConn.close({ kill: \"SIGKILL\" });"
  },
  {
   "id": "opusA-tone-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/environment-and-internals.md:132-135",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "The driver validates `--report-file` off the raw command line, before the seat file is expanded and before anything is spawned, so a bad path costs only a usage error. It wants an absolute path, a writable parent created at 0700 when missing, and an unused name. Nothing prunes what it makes; the caller owns it.",
     "words": 55
    },
    "off": {
     "text": "The driver validates `--report-file` off the raw command line, before the seat file is expanded and before anything is spawned. It wants an absolute path, a writable parent, created at 0700 all the way down when missing, and a name not yet in use. Nothing prunes what it makes; the caller owns it.",
     "words": 53
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "opusA-tone-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/result-gates.md:53-56",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "`--verify-sandboxed` runs the check through `codex sandbox` under the read profile, which keeps the check as honest as the seat: the tree is readable, `$TMPDIR` writable, a tree-writing verifier fails, and the network matches the seat's, so a denied seat cannot be confirmed by a fetch. `verify.sandboxed` records the mode.",
     "words": 50
    },
    "off": {
     "text": "`--verify-sandboxed` runs the check through `codex sandbox` under the read profile. The tree is readable, `$TMPDIR` is writable, and a verifier that writes into the tree fails. Its network is whatever the seat's was, so a seat denied egress gets a verifier denied it too. `verify.sandboxed` records the mode.",
     "words": 49
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "opusA-tone-3",
   "type": "single-factor",
   "source": "research/2026-09-10-chain/chain/audit.md:164-174",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "Claims C14 and C15 cover two rows of the rights table, each anchored where it counts. C14, at README line 54, states read access to every readable path, TMPDIR writes, network and commands; C15, at line 55, the managed detached worktree. Three `driver.mjs` sites back each row, so neither rests on assertion.",
     "words": 52
    },
    "off": {
     "text": "Claims C14 and C15 cover two rows of the rights table. C14, at README line 54, states read access to every readable path, TMPDIR writes, network and commands; C15, at line 55, states the managed detached worktree. Each of the two rows cites three separate sites in `driver.mjs`.",
     "words": 48
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "opusA-tone-4",
   "type": "single-factor",
   "source": "research/2026-09-10-chain/chain/audit.md:218-224",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "Claim C23, at README line 71, covers the fresh worktree's HEAD and the exclusion of the live tree's uncommitted, untracked and ignored files. `driver.mjs:1688-1717` backs it, and a short derivation closes the rest neatly: `git worktree add --detach` takes no commit argument for a fresh seat, and stashing does not move HEAD.",
     "words": 52
    },
    "off": {
     "text": "Claim C23, at README line 71, covers the fresh worktree's HEAD and the exclusion of the live tree's uncommitted, untracked and ignored files. One site backs it, `driver.mjs:1688-1717`, and the remainder is derived: `git worktree add --detach` takes no commit argument for a fresh seat, and stashing does not move HEAD.",
     "words": 51
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "opusA-metaphor-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:485-488",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "A seat that delegates comes back exit 5, \"no command ran\", still carrying its answer: only the root thread is evidence, and the answer is still the seat's. The report's `subagentThreads` did not count those children, which never arrive as a `thread/started` with a `parentThreadId`. This release counts them, and the exit-5 cause names them.",
     "words": 55
    },
    "off": {
     "text": "A seat that delegates comes back exit 5, \"no command ran\", still holding its answer: only the root thread is a witness, and the answer is still the seat's. The report's `subagentThreads` was blind to those children, which never arrive as a `thread/started` with a `parentThreadId`. This release opens its eyes, and the exit-5 cause names them.",
     "words": 57
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "opusA-metaphor-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/environment-and-internals.md:149-152",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "The provenance fields listed under Report in `--help-all` come from that record, and `receiptWhy` says why validation failed. A `receiptOk: false` beside a claimed success is a reason to distrust the claim. A process that can fabricate the whole report can fabricate these fields too, so open the rollout yourself when the answer needs stronger assurance.",
     "words": 56
    },
    "off": {
     "text": "The provenance fields listed under Report in `--help-all` come from that record, and `receiptWhy` says why validation failed. A `receiptOk: false` beside a claimed success is a red flag. A process that can fabricate the whole report can fabricate these fields too, so open the rollout yourself when the answer has to hold water.",
     "words": 54
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "opusA-metaphor-3",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/result-gates.md:77-79",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "The other `verifySkipped` values are not exit 12, because an earlier rule has already decided the code: a budget cut is exit 3, and a turn that never completed is exit 2 when the server rejected the request as invalid, exit 1 otherwise. Checking a partly written tree would only add a misleading verdict.",
     "words": 54
    },
    "off": {
     "text": "The other `verifySkipped` values are not exit 12, because the ladder has already spoken: a budget cut is exit 3, and a turn that never completed is exit 2 when the server rejected the request as invalid, exit 1 otherwise. Checking a half-written tree would only bolt a misleading verdict onto a verdict already given.",
     "words": 55
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "opusA-metaphor-4",
   "type": "single-factor",
   "source": "plugins/codex-delegate/evals/README.md:167-172",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "A suite that passes is not the same as a suite that detects defects. Check the second property by mutating the driver in a copy and confirming that the right cases fail: removing the `threadId` filter must fail the attribution cases, and moving the lock back into the cwd must fail most of the lock suite.",
     "words": 56
    },
    "off": {
     "text": "A green suite is not the same as a suite that bites. Check the second property by mutating the driver in a copy and confirming that the right cases go red: removing the `threadId` filter must fail the attribution cases, and moving the lock back into the cwd must fail most of the lock suite.",
     "words": 55
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "opusA-answer-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/evals/README.md:140-147",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "Measure the invariant, not a proxy for it. Counting how many of eight racing runs exited 0 proves nothing: runs that acquire the lock in sequence all legitimately succeed, and two reviews and one implementer still read those exit codes as a violation. What settles it is whether two runs hold the critical section at once.",
     "words": 56
    },
    "off": {
     "text": "The concurrency bug in the reclaim path was first found by counting how many of eight racing runs exited 0. But runs that acquire the lock in sequence all legitimately succeed, and two reviews and one implementer read those exit codes as a violation. So what settles it? Whether two runs hold the critical section at once.",
     "words": 57
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "opusA-answer-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/environment-and-internals.md:177-180",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "A second write run in the same directory exits 10, and so does resuming a thread whose turn is still open. At `--level write` the driver holds an exclusive lock keyed on the cwd: the second run would otherwise race the other's edits, tests and cleanup, and old events would satisfy the new invocation while its prompt went unconsumed.",
     "words": 59
    },
    "off": {
     "text": "Two write seats can be pointed at one directory, and a resume can land on a thread whose turn is still open. The first would race the other's edits, tests and cleanup; the second would let old events satisfy the new invocation while its prompt went unconsumed. What happens? Both exit 10, on an exclusive lock keyed on the cwd.",
     "words": 60
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "opusA-answer-3",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/result-gates.md:30-35",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "A completed turn that produced an answer exits 0, however many of its commands failed. The exit code cannot tell a finding — a crashing environment probe, a deliberately broken build, a bisection seat's failing step — from a defect, so it does not try. The report's command counters say what ran and how it ended; read them first.",
     "words": 59
    },
    "off": {
     "text": "Some seats fail commands on purpose: a crashing environment probe, a deliberately broken build, a bisection seat's failing step. Others fail them because something is wrong, and the exit code cannot tell those two apart. Should it guess? It does not: a completed turn with an answer exits 0, and the report's counters say what ran — read them first.",
     "words": 60
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "opusA-answer-4",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:534-539",
   "engine": "claude-opus-5",
   "variants": {
    "on": {
     "text": "The 0.8.0 relay measurement was wrong: those runs were sonnet, not haiku. No run was verified by model id, the shipped agent's `model: sonnet` frontmatter overrides `claude -p --model haiku`, and the transcript shows `claude-sonnet-4-6`. A real haiku, a copy carrying its own model line, ignored the relay contract in four runs of four.",
     "words": 54
    },
    "off": {
     "text": "On 2026-09-03 the relay was reported running on haiku, unverified by model id. The shipped agent's `model: sonnet` frontmatter overrides `claude -p --model haiku`. So which model answered? Sonnet: the transcript shows `claude-sonnet-4-6`. A real haiku, a copy carrying its own model line, ignored the relay contract in four runs of four.",
     "words": 52
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "opusA-cc-1",
   "type": "code-comment",
   "source": "plugins/codex-delegate/skills/seat/scripts/driver.mjs:2042-2044",
   "engine": "claude-opus-5",
   "variants": {
    "keep": {
     "text": "// Refuse multiple seat files rather than silently choosing which declaration supplies the run.",
     "words": 14
    },
    "rewrite": {
     "text": "// Two `--seat-file` flags are a usage error, not a choice between them.",
     "words": 13
    },
    "delete": {
     "text": "(no comment)",
     "words": 2
    }
   },
   "code": "  if (at >= 0 && argv.indexOf(\"--seat-file\", at + 2) >= 0)\n    fail(EXIT.USAGE, \"--seat-file given more than once; only one seat file defines a seat\");"
  },
  {
   "id": "opusA-cc-2",
   "type": "code-comment",
   "source": "plugins/codex-delegate/skills/seat/scripts/driver.mjs:1872-1876",
   "engine": "claude-opus-5",
   "variants": {
    "keep": {
     "text": "// A patch reproduces content, not history; give the seat's commits a permanent, reported ref\n// before removing the detached worktree that otherwise holds their only reference.",
     "words": 27
    },
    "rewrite": {
     "text": "// Removing the detached worktree drops the only reference to these commits, so name them under refs/codex-delegate/ first; the diff carries content, not history.",
     "words": 24
    },
    "delete": {
     "text": "(no comment)",
     "words": 2
    }
   },
   "code": "      if (committed) {\n        const ref = `refs/codex-delegate/${name}`;\n        const upd = git(repo, [\"update-ref\", ref, headSha]);"
  },
  {
   "id": "sonD-tone-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/README.md:23-26",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "A second, user-invoked skill, `/codex-delegate:orchestrate`, turns the whole session into an orchestrator: it scouts inline, agrees one plan, then pushes every verbose step onto Claude and Codex seats. It stays prompt only, adding no flag or field, a delta worth having over the base skill.",
     "words": 45
    },
    "off": {
     "text": "A second, user-invoked skill, `/codex-delegate:orchestrate`, turns the whole session into an orchestrator: it scouts inline, agrees one plan, then pushes every verbose step onto Claude and Codex seats. It is prompt only, adding no flag or field, a delta over the base skill.",
     "words": 43
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "sonD-tone-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/orchestrate/SKILL.md:137-138",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "Every prompt seat, Claude or Codex, is asked for exactly five fields; no `BRIEF:` line is sent, because the template bounds the reply, and `BRIEF:` would clip the answer at 20 lines. The first line of `result` stands alone: model, id, status, what happened. All five stay read-only, never forwarded, keeping the record honest.",
     "words": 54
    },
    "off": {
     "text": "Every prompt seat, Claude or Codex, is asked for exactly five fields; no `BRIEF:` line is sent, because the template already bounds the reply and `BRIEF:` would clip it at 20 lines. The first line of `result` stands alone: model, id, status, what happened. All five fields are read, never forwarded.",
     "words": 51
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "sonD-metaphor-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/incidents.md:85-86",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "A review probe started eight busy loops to test timeout behavior under CPU load, placing the cleanup command after the measurement step. Its parent process exited first, so the loops were reparented to PID 1 and used eight of twelve cores continuously for fifteen hours.",
     "words": 45
    },
    "off": {
     "text": "A review probe fired off eight busy loops to test timeout behavior under CPU pressure, tucking the cleanup command in after the measurement step. Its parent died first, so the loops were orphaned onto PID 1 and burned eight of twelve cores for fifteen hours.",
     "words": 45
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "sonD-metaphor-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/README.md:3-5",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "A Claude Code plugin whose main skill delegates coding work to OpenAI Codex as a subagent, with the rights for each call declared up front: analysis that reads and runs but writes nothing of yours, or writing and running tests inside a git worktree the driver manages itself. Every completed turn produces a receipt.",
     "words": 54
    },
    "off": {
     "text": "A Claude Code plugin whose main skill hands coding work off to OpenAI Codex as a subagent, with the rights for each call declared up front: analysis that reads and runs but writes nothing of yours, or writing and running tests inside a git worktree the driver manages itself. Every completed turn leaves a receipt.",
     "words": 55
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "sonD-answer-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/incidents.md:101-104",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "Report a failed-command count as evidence, not a verdict: pass `--verify` with the end condition you actually want. That fix follows an incident where three mutation-testing seats ran suites against deliberately broken copies, and `commandsFailed` read 24, 17 and 9 while the run announced failure for work that had actually succeeded.",
     "words": 51
    },
    "off": {
     "text": "Three mutation-testing seats once ran suites against deliberately broken copies; `commandsFailed` read 24, 17 and 9, yet the run announced failure for work that had actually succeeded. Should a failed-command count ever stand as the verdict? No: treat it as a report field, and pass `--verify` with the end condition you want.",
     "words": 52
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "sonD-answer-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/incidents.md:108-110",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "A non-zero exit reflects gate results only, not whether an answer exists: read the answer, and gate solely on its evidence. That rule exists because a review panel's merge step once discarded a receipt-verified Codex answer holding ten findings, simply because one failed probe made the run exit 11.",
     "words": 49
    },
    "off": {
     "text": "A review panel's merge step once discarded a receipt-verified Codex answer holding ten findings, because one failed probe made the run exit 11. Does a non-zero exit mean no answer exists? No: it only reflects the gates. Read the answer, and gate solely on its evidence.",
     "words": 46
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "sonA-tone-1",
   "type": "single-factor",
   "source": "research/2026-09-10-chain/chain/cut-ledger.md:28-30",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "A coordinator agent, Sonnet or Opus, that has loaded this skill launches a Codex subagent and gets a finished, verifiable answer back on the first attempt, which matters because nothing needs configuring beforehand and nothing needs to be known in advance to succeed.",
     "words": 43
    },
    "off": {
     "text": "A coordinator agent, Sonnet or Opus, that has loaded this skill launches a Codex subagent and gets a finished, verifiable answer back on the first attempt. Nothing needs configuring beforehand, and nothing needs to be known in advance to make the call.",
     "words": 42
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "sonA-tone-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:669-671",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "This release adds npm test across seven suites with a shared harness, generated driver constants, and generated exit-ladder help text, so nothing drifts. CI now spans six free suites on Linux and macOS with Node 18 and 24, and --allow-failed-commands lets expected probe failures pass, catching regressions early.",
     "words": 48
    },
    "off": {
     "text": "This release adds npm test across seven suites with a shared harness, generated driver constants, and generated exit-ladder help text. It runs CI for six free suites on Linux and macOS with Node 18 and 24, and adds --allow-failed-commands to pass through expected probe failures.",
     "words": 45
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   },
   "repeat": true
  },
  {
   "id": "sonA-metaphor-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/orchestrate/SKILL.md:12-14",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "Load the seat skill now and follow it for every Codex seat: rights, header fields, worktree lifecycle, the report, and the exit code list live there and stay authoritative. This page only covers what the orchestrate mode changes, leaving the rest of that skill's content unchanged.",
     "words": 46
    },
    "off": {
     "text": "Load the seat skill now and follow it for every Codex seat: rights, header fields, worktree lifecycle, the report, and the exit ladder live there and stay authoritative. This page only re-cuts what the orchestrate mode changes, leaving the rest of that skill's ground untouched.",
     "words": 45
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "sonA-metaphor-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:647-649",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "The driver's own git operations became unaffected by hooks, fsmonitor, text conversion, and external diffs. Intent is now recorded before checkout and refs are retained before cleanup. Abandoned MCP directories get removed, and finished worktree threads can resume by rebuilding their previously collected content.",
     "words": 44
    },
    "off": {
     "text": "The driver's own git operations became immune to hooks, fsmonitor, text conversion, and external diffs. Intent is now recorded before checkout and refs are retained before cleanup. Abandoned MCP homes get reaped, and finished worktree threads can resume by rebuilding their harvested content.",
     "words": 43
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   },
   "repeat": true
  },
  {
   "id": "sonA-answer-1",
   "type": "single-factor",
   "source": "research/2026-09-10-chain/chain/cut-ledger.md:32-36",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "Rights are declared per call and exit codes come from evidence, not assumption, so the coordinator never has to guess what a seat touched or whether it truly did the work. Defaults win over conflicting knobs. Any rule a caller must already know to succeed is this repository's defect, not the coordinator's.",
     "words": 52
    },
    "off": {
     "text": "A coordinator calling a seat cannot see inside the call: what did the seat touch, did a silent seat really do nothing, and which wins when a knob and a default disagree? Rights are declared per call, exit codes follow from evidence, defaults beat knobs, and any rule a caller must already know is this repository's defect, not the coordinator's.",
     "words": 60
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "sonA-answer-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/environment-and-internals.md:293-297",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "The driver's read-level assert checks the effect, not just the profile name: sandbox type workspaceWrite, the requested network access, the cwd present in runtimeWorkspaceRoots, and writableRoots equal to exactly $TMPDIR, or empty when --cwd is $TMPDIR itself. It checks this because a name-only check, run first, would pass whether or not the grant actually applied.",
     "words": 55
    },
    "off": {
     "text": "A profile's id is asserted first, but a name-only check would pass whether a grant actually took effect or not, so what tells the two cases apart? The driver's read-level assert checks the effect itself: sandbox type workspaceWrite, the requested network access, the cwd in runtimeWorkspaceRoots, and writableRoots equal to exactly $TMPDIR, or empty when --cwd is $TMPDIR itself.",
     "words": 59
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   },
   "repeat": true
  },
  {
   "id": "sonB-tone-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:693-696",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "This release hardened seat files against tampering: `SEAT` must come first, a relayed `VERIFY` needs command-line authorization, and declared fields are reported, closing an admission gap. Strict output schemas became mandatory and rollout receipts are validated via `session_meta`, so an unmeasured verifier now fails safely with exit 12.",
     "words": 48
    },
    "off": {
     "text": "This release hardened seat files: the `SEAT` declaration must come first, a relayed `VERIFY` needs command-line authorization, and declared fields are reported. It also made strict output schemas mandatory, made an unmeasured verifier exit with code 12, and validated rollout receipts by opening their `session_meta` record.",
     "words": 46
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "sonB-tone-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:705-709",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "This release shipped the repository as a Claude Code plugin with the `codex-seat` relay agent, a safer foundation: `--seat-file` lets wrappers pass literal fields instead of interpolating user values into a shell command, rejecting unknown or repeated fields, while identity-based root guards, strict schema-verdict handling, and refused-retry report integrity are verified by adversarial contract tests.",
     "words": 55
    },
    "off": {
     "text": "This release shipped the repository as a Claude Code plugin with the `codex-seat` relay agent, added `--seat-file` so wrappers pass literal fields instead of interpolating user values into a shell command, rejected unknown or repeated fields, and added identity-based root guards, strict schema-verdict handling, and refused-retry report integrity, with adversarial contract tests.",
     "words": 52
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "sonB-metaphor-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/orchestrate/SKILL.md:50-53",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "A Codex worktree seat cannot commit under the rights a `SEAT:` line grants: its restricted rights end at the tree, so its work comes back as a diff. Apply the finished work as a proposal: apply `worktreeDiffPath`, restore `worktreeUntrackedPath`, or merge `worktreeCommitsRef` when the seat committed, then wait unless the plan said to apply the winning result.",
     "words": 57
    },
    "off": {
     "text": "A Codex worktree seat cannot commit under the rights a `SEAT:` line grants: its sandbox ends at the tree, so its work comes back as a diff. Land the harvest by proposal: apply `worktreeDiffPath`, restore `worktreeUntrackedPath`, or merge `worktreeCommitsRef` when the seat committed, then wait unless the plan said \"land the winner\".",
     "words": 52
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "sonB-metaphor-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/incidents.md:8-12",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "Of 157 delegations run against the caller's own `~/.codex`, 95 used their first tool call to read `~/.codex/plugins/cache` instead of the task; 209 of 919 tool calls went there, and one turn ended having only announced it had to run a plugin's workflow first. Hence the private `CODEX_HOME`.",
     "words": 48
    },
    "off": {
     "text": "Of 157 delegations run against the caller's own `~/.codex`, 95 spent their first tool call reading `~/.codex/plugins/cache` instead of the task; 209 of 919 tool calls went there, and one turn ended having only announced it had to run a plugin's workflow first. Hence the private `CODEX_HOME`.",
     "words": 47
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "sonB-answer-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/incidents.md:23-27",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "A seat that did nothing is indistinguishable from a seat that found nothing. A wrapper once started a background Codex job and returned at once with an unknown suites-pass status and no findings, yet the panel still counted it as reported. A later seat's returned task id matched no rollout; a receipt check would have caught it in milliseconds.",
     "words": 59
    },
    "off": {
     "text": "A wrapper once started a background Codex job and returned at once with an unknown suites-pass status and no findings, yet the panel still counted it as reported. A later seat's returned task id matched no rollout; a receipt check would have caught it in milliseconds. A seat that did nothing is indistinguishable from a seat that found nothing.",
     "words": 59
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "sonB-answer-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/incidents.md:16-19",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "Both rules are now unconditional. One agent ran the Codex seat first under an unstated mix and disclosed it only in the write-up, when the rule still applied only if the caller named a ratio. Another, told \"no codex,\" ran none but never said so, reading it as \"do not open the Codex skill.\"",
     "words": 54
    },
    "off": {
     "text": "Under an unstated mix, one agent ran the Codex seat first and disclosed it only in the write-up, when the rule still applied only if the caller named a ratio. Another, told \"no codex,\" ran none but never said so, reading it as \"do not open the Codex skill.\" Both rules are now unconditional.",
     "words": 54
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "sonC-tone-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/CHANGELOG.md:722-727",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "The release adds a private CODEX_HOME so calls stay safely isolated, yet it inherits the resolved model, effort, personality, and service tier from config/read — nothing to reconfigure. It adds web-search modes, JSON answers, answer logging, and protocol, lock, and fidelity suites against codex-cli 0.150.1, and reshapes the report so a fast, capped answer never hides the full text at answerPath.",
     "words": 61
    },
    "off": {
     "text": "The release adds a private CODEX_HOME that isolates each call while still inheriting the resolved model, effort, personality, and service tier from config/read. It adds web-search modes, JSON answers, answer logging, and protocol, lock, and fidelity suites against codex-cli 0.150.1, and reshapes the report to cap the inline answer while keeping the full text at answerPath.",
     "words": 56
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "sonC-tone-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/orchestrate/SKILL.md:104-104",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "A Codex seat is a single background Bash task, which keeps it easy to track: it gets its own scratch directory for the prompt and output files, and a report path the driver creates, so results stay organized. It is not an agentType, avoiding hidden routes, and you stop it cleanly by stopping its task.",
     "words": 55
    },
    "off": {
     "text": "A Codex seat is a single background Bash task: it gets its own scratch directory for the prompt and output files, and a report path under the run directory that the driver creates. It is not an agentType, so there is no other route to one. Stopping it means stopping its task.",
     "words": 52
    }
   },
   "factor": "promotional-tone",
   "instructions": {
    "on": "state what it does and why it is good",
    "off": "state what it does"
   }
  },
  {
   "id": "sonC-metaphor-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/incidents.md:43-46",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "A driver-side effort default silently downgraded a config asking for `max` to `low` on every delegation, so effort and model now inherit unless flagged. `minimal` was once refused as a usage error even though the server accepted it; the range of levels is now a permissive union, and a value the server refuses exits 2 with its own list.",
     "words": 59
    },
    "off": {
     "text": "A driver-side effort default silently downgraded a config asking for `max` to `low` on every delegation, so effort and model now inherit unless flagged. `minimal` was once refused as a usage error even though the server accepted it; the ladder is now a permissive union, and a value the server refuses exits 2 with its own list.",
     "words": 57
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "sonC-metaphor-2",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/seat/references/incidents.md:51-53",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "Four distinct defects surfaced in one day: a single-quoted value matched nothing; a multi-line string body was scanned as settings; a duplicate key was emitted, which codex rejects outright; and a comment merely mentioning `= \"\"\"` opened a skip that discarded every setting after it.",
     "words": 45
    },
    "off": {
     "text": "Four distinct defects surfaced in one day: a single-quoted value matched nothing; a multi-line string body was scanned as settings; a duplicate key was emitted, which codex rejects outright; and a comment merely mentioning `= \"\"\"` opened a skip that swallowed every setting after it.",
     "words": 45
    }
   },
   "factor": "metaphor",
   "instructions": {
    "on": "use the literal phrase where one exists",
    "off": "no constraint on figurative language"
   }
  },
  {
   "id": "sonC-answer-1",
   "type": "single-factor",
   "source": "plugins/codex-delegate/skills/orchestrate/SKILL.md:81-83",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "By default, half the seats beyond the implementers, rounded up, go to Codex, covering judgement roles: plan critique, review, skeptics and refuters, judges. That default holds only when the user states no allocation. A one-seat task has no judgement seat at all, so it gets a Codex seat only if cross-review adds one.",
     "words": 53
    },
    "off": {
     "text": "A task with no stated seat allocation still needs judgement roles filled: plan critique, review, skeptics and refuters, judges. But a one-seat task has no judgement seat at all, so how many seats, if any, go to Codex? By default, half the seats beyond the implementers, rounded up, are Codex; a one-seat task gets one only if cross-review adds it.",
     "words": 60
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "sonC-answer-2",
   "type": "single-factor",
   "source": "research/2026-09-10-chain/chain/cut-ledger.md:85-89",
   "engine": "claude-sonnet-5",
   "variants": {
    "on": {
     "text": "Both macOS and Linux are covered by CI: every suite that needs no codex binary runs on each, while the macOS-only call — the managed-preferences plist — is guarded. On a stock Linux shell, TMPDIR is normally unset, so at --level read the driver makes its own and reports it as tmpDir; export yours to relocate the seat's scratch files.",
     "words": 60
    },
    "off": {
     "text": "A Linux shell leaves TMPDIR unset, and at --level read that variable is the grant, so how does CI confirm both platforms behave? The driver makes its own and reports it as tmpDir; export yours to relocate scratch files. Both macOS and Linux are measured: every suite needing no codex binary runs on each, and the macOS-only call is guarded.",
     "words": 60
    }
   },
   "factor": "answer-first",
   "instructions": {
    "on": "open with the answer",
    "off": "open with situation, complication, question"
   }
  },
  {
   "id": "whole-1",
   "type": "whole-text",
   "factor": "",
   "source": "research/2026-09-10-chain/chain/00-original.md:165-179",
   "engine": "none-extracted",
   "code": "",
   "instructions": {
    "before": "the section as it stood on 2026-09-10",
    "after": "the same section after the audit-and-revise chain ran on it"
   },
   "variants": {
    "before": {
     "text": "| Call | Codex may |\n| --- | --- |\n| `--level read` (the default; `--cwd DIR` is optional and defaults to the current directory) | read any readable path, reach the network, run commands, write only `$TMPDIR` — enough to run tests |\n| `--worktree REPO` | write level in a managed detached tree the driver creates, harvests and removes; what it starts from and lacks is in [parity.md](skills/seat/references/parity.md#read-and-isolated-write) |\n| `--level write --cwd DIR` | write anywhere under a directory you chose |\n| `+ --writable DIR` / `--no-network` | an extra root, an explicit opt-in; or a sandbox that reaches nothing |\n\nEgress is on at both levels, as it is for a native subagent, and no host list narrows it; `--no-network`\nis what takes it away, from the sandbox — the provider's own web search is a separate channel, off until\n`--web-search` asks for it, and a denied sandbox and a granted search mode are accepted together. Egress\nmoves nothing on disk — a read seat still writes only `$TMPDIR` — but whatever a seat can read it can\nsend, and at read level that is every path you can read.",
     "words": 196
    },
    "after": {
     "text": "`DIR` is a folder path; `REPO` is a Git repository. `--cwd` chooses the working folder.\n`$TMPDIR` names the writable temporary directory. A worktree is a separate Git checkout.\n\n| Call | Codex may |\n| --- | --- |\n| `--level read` (the default; `--cwd DIR` is optional and defaults to the current directory) | read any readable path, reach the network, run commands, write only `$TMPDIR` — enough to run tests |\n| `--worktree REPO` | write level in a managed detached tree the driver creates, harvests and removes; what it starts from and lacks is in [parity.md](skills/seat/references/parity.md#read-and-isolated-write) |\n| `--level write --cwd DIR` | write anywhere under a directory you chose |\n| `+ --writable DIR` / `--no-network` | an extra root, an explicit opt-in; or a sandbox that reaches nothing |\n\n`--writable` requires write level. If `TMPDIR` is unset at read level, the driver creates a private\ntemporary directory and reports it as `tmpDir`. If you set it yourself, its contents are writable too.\n\nNetwork access is on at both levels, with no host allowlist. Whatever Codex can read, it can send;\nat read level that includes every readable path. `--no-network` denies network access from the\nsandbox. The provider's web search is separate: it is disabled unless `--web-search` selects a mode.\nThe driver accepts sandbox network denial together with an allowed web-search mode.\n\nThe driver checks the server's reported sandbox, writable roots, network setting and approval policy.\nA mismatch refuses the run. An approval request during the turn is refused and recorded.\n\nA fresh worktree starts at `HEAD`, the latest commit. It excludes uncommitted edits, untracked and\nignored files, and locally installed dependencies. Commit relevant changes before choosing it;\nstashing them alone does not put them into `HEAD`.\n\nAfter a completed turn, the driver saves tracked changes as `worktreeDiffPath` and non-ignored untracked\nfiles as `worktreeUntrackedPath`, then attempts to remove the tree. Ignored files are not archived.\nAn incomplete turn or a failed harvest or removal leaves the tree preserved. Inspect the worktree\nfields even on exit 0: the exit verdict does not test whether harvesting succeeded.",
     "words": 349
    }
   },
   "rewriteOf": "research/2026-09-10-chain/chain/03-prerequisite-pass.md:47-78"
  },
  {
   "id": "whole-2",
   "type": "whole-text",
   "factor": "",
   "source": "research/2026-09-10-chain/chain/00-original.md:187-207",
   "engine": "none-extracted",
   "code": "",
   "instructions": {
    "before": "the section as it stood on 2026-09-10",
    "after": "the same section after the audit-and-revise chain ran on it"
   },
   "variants": {
    "before": {
     "text": "- **Exit codes from evidence.** The `codex` process always exits 0; the driver derives an ordered\n  ladder of exit codes from the event stream. The driver's `--help` is the complete ladder;\n  `SKILL.md` gives the decisions a coordinator makes on it.\n- **Evidence gates.** `--verify '<shell>'` runs after the turn, executed by the driver, never authored by\n  the model — but with the coordinator's own rights, env and network, so a verifier that executes tree\n  contents (`npm test` runs the seat's `package.json` script) is running the seat's code; prefer one\n  that does not, or add `--verify-sandboxed` to put it behind the read profile, which confines its\n  writes to `$TMPDIR` and hands it the seat's own egress, a denial included;\n  `--expect-command <regex>` demands the work matched a declared signature;\n  `--output-schema <file>` demands a JSON answer matching a schema. Semantics, and how each gate can\n  be fooled: the driver's `--help` and [references/result-gates.md](skills/seat/references/result-gates.md).\n- **Sandbox asserted, not assumed.** The rights the server reports are compared against the rights that\n  were asked for, and a mismatch refuses the run instead of proceeding under an unknown sandbox.\n- **A receipt per run.** `receiptPath`/`receiptOk` locate the rollout and check it names this thread;\n  what that does and does not prove is in\n  [the internals reference](skills/seat/references/environment-and-internals.md#receipt-validation-and-reporting).\n- **Isolation by default.** Runs use a private `CODEX_HOME`, so your plugins, skills and MCP servers\n  stay out of the turn and no trust records are written back; `--host-home` opts out.",
     "words": 239
    },
    "after": {
     "text": "Exit 0 means the turn completed, supplied an answer and passed the driver's applicable checks.\nRead the report and inspect the cited files before accepting the answer.\n\nBy default, the report must contain at least one command item from the main Codex conversation,\ncalled a **thread**. Commands reported only by Codex's own subagents do not count toward that check. A failed command or one with an\nunknown outcome can satisfy the default count; look at its `status` and `exitCode`.\n\n**`--allow-no-commands` or `ALLOW_NO_COMMANDS: yes` waives that count. Exit 0 can then mean no command\nwas reported.** Use it when commands are unnecessary, such as recalling an earlier answer. It does not\nwaive a declared `--expect-command` check.\n\n| Report field | What to inspect |\n| --- | --- |\n| `commands` | Each main-thread command's text, status and exit code. Failed commands do not by themselves make the run fail. |\n| `commandsFailed`, `commandsBlocked`, `commandsPipedToPager` | Failures, unknown command outcomes and commands whose final pipe stage may hide output or supply the pipeline's exit status. |\n| `answer`, `answerPath` | The answer and its saved full text. `answerPath` can be null if there was no answer or saving failed. |\n| `receiptPath`, `receiptOk`, `receiptWhy` | A session-record location, whether its opening metadata names this thread, and why that check failed. |\n\nA **receipt** is Codex's saved session record, also called a rollout. The driver searches today's and\nyesterday's session directories and checks only the opening `session_meta` record, within a 64 KiB\nread. `receiptOk: true` establishes that this metadata names the reported thread. It does not validate\ncommands, task completion or the answer. **`receiptOk: false` does not change the exit verdict.** An\nolder resumed session can be outside the search window; inspect `receiptWhy` and the record.\n\nChoose automated checks before the run. These are driver flags; the seat-file equivalents are\n`EXPECT:` and `OUTPUT_SCHEMA:`. The driver refuses `VERIFY:` in a seat file unless the caller also\npasses `--allow-seat-verify`.\n\n- `--expect-command '<regex>'`: require a completed main-thread command with exit 0 whose text matches\n  the pattern. Text can mislead: `true # vitest` matches `vitest` without running it, and a pipeline can\n  return its final command's status.\n- `--verify '<shell command>'`: have the driver run your check after a completed turn. By default it\n  uses the caller's rights, environment and network. Running a script Codex changed executes that script\n  with those rights. Add `--verify-sandboxed` to restrict writes to `$TMPDIR` and use the seat's network\n  setting. A check that needs to write into the working folder cannot use that read profile.\n- `--output-schema <file>`: constrain the JSON answer's structure. The report lists schema keywords the\n  driver's validator did not recheck. Structure does not establish factual correctness.\n\nThe [result-gates reference](skills/seat/references/result-gates.md) explains the limits.\n`node skills/seat/scripts/driver.mjs --help` lists the exit codes; run it from the checkout or plugin root.",
     "words": 470
    }
   },
   "rewriteOf": "research/2026-09-10-chain/chain/03-prerequisite-pass.md:79-121"
  },
  {
   "id": "whole-3",
   "type": "whole-text",
   "factor": "",
   "source": "research/2026-09-10-chain/chain/00-original.md:208-217",
   "engine": "none-extracted",
   "code": "",
   "instructions": {
    "before": "the section as it stood on 2026-09-10",
    "after": "the same section after the audit-and-revise chain ran on it"
   },
   "variants": {
    "before": {
     "text": "The official `openai-codex` plugin is architecturally the same idea and richer in places — background\njobs, resume UX, a stop-time review gate. It is not a substitute where rights matter: it hardcodes an\napproval policy that managed (MDM) machines clamp into deny-everything, and it always sends an explicit\n`sandbox` parameter, which suppresses the permission profile that makes read-level test runs possible —\non every machine, managed or not. Both defects are silent: the run still exits 0. The `codex\nexec`-based skills and the official SDK hit the same walls. Full forensics, upstream issue state, and\nwhat the plugin does better: [references/why-not-the-plugin.md](skills/seat/references/why-not-the-plugin.md).",
     "words": 101
    },
    "after": {
     "text": "The driver requests `on-request` approval policy and omits the explicit sandbox parameter at read\nlevel so the temporary-directory grant can apply. It checks the reported permissions before proceeding.\n\nThe [comparison with the official plugin](skills/seat/references/why-not-the-plugin.md) records tests\ndated **2026-08-31**, against official plugin **1.0.6** and Codex CLI **0.150.1**. It covers managed-device\napproval failures and loss of temporary-directory writes, plus features the official plugin offered.\nCheck current upstream behavior before using that comparison to choose an integration.",
     "words": 74
    }
   },
   "rewriteOf": "research/2026-09-10-chain/chain/03-prerequisite-pass.md:258-267"
  },
  {
   "id": "whole-4",
   "type": "whole-text",
   "factor": "",
   "source": "research/2026-09-10-chain/chain/00-original.md:180-186",
   "engine": "none-extracted",
   "code": "",
   "instructions": {
    "before": "the section as it stood on 2026-09-10",
    "after": "the same section after the audit-and-revise chain ran on it"
   },
   "variants": {
    "before": {
     "text": "A run lives exactly as long as its call: there is no run registry and no collector, and the caller that\nstarted a seat owns its lifetime. `--report-file` is the delivery that survives a broken pipe, and a\nseat is stopped by `SIGTERM` to the pid the driver prints on its first stderr line — the turn is\ninterrupted, the report it had earned is written anyway, and the codex process group is swept.",
     "words": 74
    },
    "after": {
     "text": "There is no elapsed-time limit by default. The defaults cut a run after 900 seconds without qualifying\nactivity or at 1,000 recorded main-thread command items. The caller can set `--timeout`,\n`--idle-timeout` and `--max-commands`.\n\nStop the background task or send `SIGTERM` to the driver's process id on its first diagnostic line.\nThe driver attempts to interrupt the turn, deliver the available report and stop Codex's process\ngroup. Continue with `--resume <threadId>` or `RESUME: <threadId>` in a seat file.\nA still-running thread can refuse continuation with exit 10. Read any retained answer even on a\nnonzero exit.",
     "words": 95
    }
   },
   "rewriteOf": "research/2026-09-10-chain/chain/03-prerequisite-pass.md:217-228"
  },
  {
   "id": "whole-5",
   "type": "whole-text",
   "factor": "",
   "source": "research/2026-09-10-chain/chain/00-original.md:218-227",
   "engine": "none-extracted",
   "code": "",
   "instructions": {
    "before": "the section as it stood on 2026-09-10",
    "after": "the same section after the audit-and-revise chain ran on it"
   },
   "variants": {
    "before": {
     "text": "Read level cannot run browser-mode tests: Chromium needs a one-file override at the tree root, which is\na write a read seat does not have — they run at write level\n([Browser-mode sandbox](skills/seat/references/parity.md#browser-mode-sandbox)). Node-environment vitest at read level\nneeds `--configLoader runner`. Concurrency is memory-bound (figures in\n[parity.md](skills/seat/references/parity.md#fan-out-and-reporting)) and exceeding the machine\nbudget gets runs killed by the OS, not throttled. The app-server protocol is `[experimental]` and\ncarries no stability promise — hence the pinned schema and the fidelity suite.",
     "words": 79
    },
    "after": {
     "text": "From the checkout or installed plugin root (the folder containing `package.json`):\n\n```bash\nnpm test\n```\n\nThe runner stops at the first failing suite. Git-dependent package cases skip when the install has no\nGit metadata. Live cases require their binaries or opt-in variables; read the counts and skip notices.\nWithout the live-turn opt-ins, these checks do not request a model turn. CI is configured for Linux\nand macOS with Node 22 and 24.\n\nAfter upgrading Codex, run:\n\n```bash\nnode evals/fidelity.test.mjs --require-live\n```\n\nThis compares the real Codex connection handshake against the test server. A missing Codex binary is\na failure with `--require-live`; without it, missing-binary cases skip. A real model turn is a separate\nopt-in; [RELEASING.md](RELEASING.md) gives the live checks.\n\nFor tests of your own project, read the [parity reference](skills/seat/references/parity.md) before\nchoosing rights. Its Node/Vitest setup uses `--configLoader runner` at read level. Its Chromium workaround\nrequires a file in the working tree, write access, a warm package store and serial execution; it is not\na general browser-testing guarantee. The local test server also needs network access. Concurrency\nmeasurements there are dated; remeasure before choosing how many agents to run together.",
     "words": 189
    }
   },
   "rewriteOf": "research/2026-09-10-chain/chain/03-prerequisite-pass.md:229-257"
  },
  {
   "id": "whole-6",
   "type": "whole-text",
   "factor": "",
   "source": "research/2026-09-10-chain/chain/00-original.md:244-278",
   "engine": "none-extracted",
   "code": "",
   "instructions": {
    "before": "the section as it stood on 2026-09-10",
    "after": "the same section after the audit-and-revise chain ran on it"
   },
   "variants": {
    "before": {
     "text": "```\nskills/seat/                     the main skill: SKILL.md (the operating manual), scripts/ (the driver\n                                 and its companions, each self-describing under --help), references/\nskills/orchestrate/SKILL.md      the orchestrator mode: a delta over the seat skill, prompt only\nskills/cleanup/SKILL.md          the cleanup mode: runs scripts/cleanup.mjs, shows its listing and\n                                 deletes what the user chose\n.claude-plugin/                  plugin + marketplace manifests\nevals/                           the suites, one file each; run-all.mjs lists them and runs them\n                                 cheapest first, lib/harness.mjs and lib/scenarios.mjs are their\n                                 shared machinery\npackage.json                     private; the Node floor and `npm test`\n.github/workflows/ci.yml         the suites that need no `codex` binary, on its OS × Node matrix\nschema-<version>/                the files evals/conformance.test.mjs loads out of the pinned protocol\n                                 schema; kept in the repo (and therefore in plugin installs) because\n                                 those are what the suites this README tells you to run validate\n                                 against. The full generated tree is not kept here: the commit named in\n                                 the upgrade recipe holds the last one (7364f7b for 0.153.4), and the\n                                 recipe diffs the next regeneration against it.\n```\n\nCanonical homes for repeated stories:\n\n| Subject | Canonical home |\n| --- | --- |\n| composition, rights, workflow | [`SKILL.md`](skills/seat/SKILL.md) |\n| orchestration: tiers, Codex share, seat bounds, returns | [`skills/orchestrate/SKILL.md`](skills/orchestrate/SKILL.md) |\n| what the plugin leaves behind, and removing it | [`skills/cleanup/SKILL.md`](skills/cleanup/SKILL.md), `node skills/seat/scripts/cleanup.mjs --help` |\n| flags and field formats | `node skills/seat/scripts/driver.mjs --help` (`--help-all` for the rest) |\n| environment, seat files, receipts, worktree internals | [`environment-and-internals.md`](skills/seat/references/environment-and-internals.md) |\n| native capability parity and dated measurements | [`parity.md`](skills/seat/references/parity.md) |\n| measured failures behind rules | [`incidents.md`](skills/seat/references/incidents.md) |\n| suite coverage and mutations | [`evals/README.md`](evals/README.md) |",
     "words": 260
    },
    "after": {
     "text": "| Need | File |\n| --- | --- |\n| Delegation instructions and prompt headers | [skills/seat/SKILL.md](skills/seat/SKILL.md) |\n| Driver flags and report fields | `node skills/seat/scripts/driver.mjs --help`; `--help-all` adds less common options |\n| Orchestration and cleanup | [orchestrate](skills/orchestrate/SKILL.md), [cleanup](skills/cleanup/SKILL.md) |\n| State, receipts, locks and worktrees | [environment-and-internals.md](skills/seat/references/environment-and-internals.md) |\n| Capabilities and dated measurements | [parity.md](skills/seat/references/parity.md) |\n| Recorded failures and review procedure | [incidents.md](skills/seat/references/incidents.md), [adversarial-review.md](skills/seat/references/adversarial-review.md) |\n| Test coverage and mutation ledger | [evals/README.md](evals/README.md) |\n| Plugin packaging | [.claude-plugin/](.claude-plugin/), [package.json](package.json) |\n| Changes and license | [CHANGELOG.md](CHANGELOG.md), [MIT license](LICENSE) |",
     "words": 95
    }
   },
   "rewriteOf": "research/2026-09-10-chain/chain/03-prerequisite-pass.md:286-299"
  },
  {
   "id": "whole-7",
   "type": "whole-text",
   "factor": "",
   "source": "research/2026-09-10-chain/chain/00-original.md:228-243",
   "engine": "none-extracted",
   "code": "",
   "instructions": {
    "before": "the section as it stood on 2026-09-10",
    "after": "the same section after the audit-and-revise chain ran on it"
   },
   "variants": {
    "before": {
     "text": "```bash\ncodex app-server generate-json-schema --out <tmp-new>/\ngit archive 7364f7b schema-<old-version> | tar -x -C <tmp-old>/\ndiff -r <tmp-old>/schema-<old-version>/ <tmp-new>/\n```\n\nRead the diff for anything structural. Commit `<tmp-new>/` as `schema-<new-version>/` in a commit of its\nown: that commit holds the full tree the next upgrade diffs against, so replace `7364f7b` above with its hash.\n`CODEX_DELEGATE_SCHEMA_DIR=schema-<new-version> node evals/conformance.test.mjs` validates it while\n`schema-<old-version>/` is still the pinned one; once that is green, move `PINNED_CODEX`, prune the new\ndirectory to the files [conformance](evals/conformance.test.mjs) loads in a second commit, and delete the old one. Then\n`npm test` and `node evals/fidelity.test.mjs --require-live`, inspect any fixture/live difference, and\nre-check [the dated parity reference](skills/seat/references/parity.md).",
     "words": 107
    },
    "after": {
     "text": "For maintainers updating the protocol reference:\n\n1. Generate the full new schema into a temporary directory with\n   `codex app-server generate-json-schema --out <tmp-new>/`.\n2. Compare it with the full old schema from Git, not only the subset in this checkout.\n   Commit `7364f7b` contains the full `schema-0.153.4/` tree. Extract it with\n   `git archive 7364f7b schema-0.153.4 | tar -x -C <tmp-old>/`, then compare the two trees.\n3. Commit the full new tree as `schema-<new-version>/` in its own commit and record that commit's hash\n   for the next comparison. Prune the working copy to the files loaded by\n   [conformance.test.mjs](evals/conformance.test.mjs) before running conformance: it rejects unused schemas.\n4. Run `CODEX_DELEGATE_SCHEMA_DIR=schema-<new-version> node evals/conformance.test.mjs` while the old\n   version is still pinned. Resolve structural differences, then update `PINNED_CODEX` and remove the\n   old schema directory in a second commit.\n5. Run `npm test` and the live gates in [RELEASING.md](RELEASING.md). Inspect differences and recheck\n   the dated capability measurements before release.",
     "words": 149
    }
   },
   "rewriteOf": "research/2026-09-10-chain/chain/03-prerequisite-pass.md:268-285"
  }
 ]
}